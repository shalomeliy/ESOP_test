# HANDOFF — מצב העבודה הנוכחי

**מה זה:** נקודת הכניסה לכל שיחה חדשה. שיחה נסגרת → הקובץ הזה מתעדכן → השיחה הבאה
קוראת אותו ויודעת בדיוק איפה עצרנו. **הידע חי בקבצים, לא בשיחה.**

**למה זה קיים:** סשן אחד הגיע ל-79M טוקנים ו-1,031 תורים כי שלוש גרסאות נבנו בו
בלי לסגור. כל תור קורא מחדש את כל מה שנכתב לפניו, כך שהעלות גדלה עם אורך השיחה
ולא עם קושי המשימה.

> **הקובץ הזה מחזיק את ההווה בלבד. ההיסטוריה יושבת ב-[`docs/handoff/`](docs/handoff/).**
> פוצל 14/08/2026 אחרי שהגיע ל-97,449 תווים — כלומר הקובץ שנועד לחסוך הקשר הפך
> להוצאת ההקשר הגדולה בריפו, ונטען ראשון בכל שיחה. אותו כשל בדיוק שכבר תוקן
> ב-`QA_TESTBOOK.md`, ואותו פתרון: אינדקס פעיל + ארכיון פר-גרסה + אינווריאנט
> תקציב (`test_handoff_stays_small`). **אל תאחד אותו בחזרה ואל תוסיף כאן בלוק
> היסטורי — כשגרסה נסגרת, הבלוק שלה עובר ל-`docs/handoff/<גרסה>.md`.**

עדכון אחרון: **18/08/2026**

## מצב נוכחי

| | |
|---|---|
| גרסה | **v1.2.0 — בעבודה. צעדים 1-12ב מתוך 13** · הסקירה שהחזירה REPAIR **נסגרה** |
| `VERSION` | **`1.2.0`** — עודכן בצעד 11 |
| בדיקות | `608 passed, 0 failed` (היו 601 לפני 12ב, 549 ב-v1.1.1) · 28/28 אינווריאנטים |
| git | `v1.1.1` תויגה ונדחפה. `origin/main` = `4555b1f`; קומיטי צעדים 11, 12, 12ב **טרם נדחפו** |
| מיגרציות | head = `23416d683773`. **v1.2.0 אינה מוסיפה מיגרציה** (`event_type` הוא `String` בלי `CHECK`) |
| DB חי | `esop_database.db` על head. לא נגענו בו — ראו צעד 13 |
| סנדבוקס | `qa_sandbox.db` **הוזרע מחדש 16/08** (כולל טבלת הון) + `backfill_ledger`. `python -m tools.run_sandbox --port 8001` |

**v1.1.1 סגורה לחלוטין** — commit + push (15/08) ותיוג + push (16/08). אין עליה
פעולה פתוחה.

## הצעד הבא

**צעד 13 — בניית `esop_database.db` מחדש.** הצעד האחרון בגרסה, ו**דורש אישור
נפרד ברגע הביצוע + גיבוי** (ההכרעה עצמה כבר התקבלה — ראו "החלטות עומדות").
המסלול המפורט: [`docs/plan/v1.2.0.md`](docs/plan/v1.2.0.md) §"צעד 13 — הבנייה
מחדש, במפורש". שים לב לתלות בסדר: `backfill_ledger` חייב לרוץ **אחרי**
ההזרעה, והוא מסרב לרוץ אם קיים ולו אירוע `BACKFILL` אחד.

**צעד 12ב נסגר (18/08/2026) — כל 10 ממצאי הסקירה טופלו**, שלוש ההכרעות
שנדרשו שם מנומקות למטה ב"החלטות עומדות". הדוח, ההוכחות והמיפוי לבדיקות:
[`docs/review/v1.2.0.md`](docs/review/v1.2.0.md) §"סגירת הדוח" ·
`docs/plan/v1.2.0.md` §"צעד 12ב" · `QA-120-25`..`31`.
**שתי בדיקות הרגרסיה הוכחו נכשלות על הקוד שלפני התיקון** (הוחזרו שתי הנוסחאות
הישנות → 3 כשלים; הוחזר התיקון → ירוק) — זה מה שלא היה קיים: כל 601 הבדיקות
היו ירוקות בעוד התקרה נפרצת.

**צעדים 1-12ב ✅** (הרשימה המלאה: `docs/plan/v1.2.0.md` §8) · **צעד 13 ⬜
מתחילים כאן.**

**שער אחד נשאר פתוח מ-12ב: דיף התיקון עצמו לא נסקר סקירה עצמאית** (`change-reviewer`)
— בשיחה שבה נעשה לא הופעלו סוכנים. הכרעה לשיחה הבאה: לסקור את 12ב לפני צעד 13,
או לסקור את הדיף המלא של הגרסה אחריו.

**מה נבנה בצעדים 1-11** (פירוט מלא: `docs/plan/v1.2.0.md` §8 · `docs/qa/v1.2.0.md`):
`services/buyback.py` **חדש** — מקור אחד ל-preview ולביצוע · שני האנדפוינטים ·
`cap_table.py` צד המונפק ל-replay (חתך `issue_date <= as_of` **נשאר**) ·
`project_share_issuance` עם `pending_delta` · 403→**404** אחיד · `seed_cap_table`
עם מזהים דטרמיניסטיים · מודאל `buyback-modal` על מכונת המצבים הקיימת, המודאל
הראשון בפורטל עם `role="dialog"`/פוקוס/`aria-live` (2 אינווריאנטים נועלים זאת) ·
**B11** — בדיקת AST דו-כיוונית על `PROJECTORS`, **הוכחה נכשלת** בשני הכיוונים ·
`docs/qa/v1.2.0.md` (31 מקרים, 14 סיכונים) · `VERSION`→`1.2.0` · `ARCHITECTURE.md`.

**מה נוסף ב-12ב:** נוסחת התקרה = סכום העמודה · מספרי החברה על שעון "עכשיו" +
`company_as_of` · `company_event_id` · `IntegrityError`→409 · שומר שלמות
על `shares` · 404 אחיד ב-`create_shareholder` · `min="0"` הוסר · הכפתור מנוטרל
אחרי כשל.

- **האירוע נכתב ב-`seed_data` ולא ב-`backfill_ledger`** — אחרי המעבר ל-replay
  שורת הנפקה בלי אירוע מדליקה `partial`. **`source` נשאר `LIVE` ולעולם לא
  `BACKFILL`**: `backfill_ledger.main` מסרב לרוץ אם קיים אירוע גיבוי אחד, כך
  שסימון שגוי כאן היה חוסם בשקט את שלב 3 של צעד 13.
- **הסנדבוקס נושא רכישה עצמית שבוצעה** (`SI-COMP-001-INVESTOR-SEED-2` = 150,000,
  החברה 7,015,000) — תוצר האימות הידני בצעד 10, לא באג.
- **תוצאות מצופות ב-`docs/qa/` נכתבו בכמויות ולא בתאריכים** — תאריכי ההזרעה
  נגזרים מיום ההרצה, וצעד 13 יזיז אותם ב-~23 יום.


**שתי עובדות שאין לגלות מחדש:**

1. **רשת הביטחון הוכחה, לא הונחה.** הוסר זמנית חתך `issue_date <= as_of` מקוד
   הייצור → **11 כשלים** (B1 על 7 תאריכים, B2 על 4). הוחזר, `diff` אישר זהות
   בייט-לבייט. אל תחזור על הניסוי — הוא מתועד כאן.
2. **שתי בדיקות קיימות שונו במכוון** (`403`→`404` בגישה חוצת-חברות, מפרט §7 +
   קריטריון 8). הן לא הוחלשו אלא חוזקו: כל אחת בודקת עכשיו מזהה-של-חברה-אחרת
   **וגם** מזהה מומצא, ומאמתת שגוף התשובה **זהה** — בדיקה שרק סופרת 404 הייתה
   מחזירה את אורקל הקיום דרך טקסט השגיאה.

**המפרט אושר (15/08/2026) ועבר שתי סקירות עצמאיות (16/08), 16 קריטריונים:**
[`docs/spec/v1.2.0.md`](docs/spec/v1.2.0.md) — שם, ולא כאן, יושבים שבעת
החוסמים שנסגרו (בראשם ח1: `partial` שווא על דאטה בריאה, שהוא הנימוק לכך שחתך
`issue_date <= as_of` **חייב** להישאר בשאילתת ה-replay).

הגרסה **פוצלה** ל-1.2.0 (תשתית: סוג אירוע ledger שני + מעבר הדילול ל-replay +
רכישה עצמית + הזרעת טבלת הון) · 1.2.1 (מיזוג/גיוס/העברה) · 1.2.2 (האצה +
סימולטור אקזיט). `FEATURE_SPEC.md` עודכן בהתאם.

**v1.1.1 (פריטים ב+ג+ד) — סגורה ומאורכבת:**
[`docs/handoff/v1.1.1.md`](docs/handoff/v1.1.1.md) · `docs/qa/v1.1.1.md`.
**פריט א (`Float` → `Decimal`) נשאר פתוח** — ראו "חוב פתוח" למטה.

## החלטות עומדות — לא לפתוח מחדש

- **`esop_database.db` ייבנה מחדש בסוף v1.2.0 (הכרעת המשתתף, 16/08/2026 —
  הופכת את "סנדבוקס בלבד" מ-15/08).** נמדד: הדאטה הדומיינית (25/260/251/19/247)
  משוחזרת זהה והמזהים יציבים; האובדן הוא 7 בקשות מימוש ידניות, 2 אישורים,
  הפקדת נאמן ו-14 שורות audit. **סיכון מקובל:** כל תאריך זז ~23 יום וכל אחוז
  הבשלה איתו; `docs/qa/` המאורכב **לא יתוקן**. הצעד הוא **האחרון** בגרסה
  (צעד 13 בתוכנית), ודורש אישור נפרד ברגע הביצוע + גיבוי.
- **שלוש הכרעות צעד 12ב (17/08/2026) — לא לפתוח מחדש.** (1) **מספרי הכותרת
  ברמת החברה הם מספרי "עכשיו"** ולא ב-`effective_date`, מוחזרים עם
  `company_as_of` ומתויגים; הנימוק: הפיצ'ר מתעד עסקה שקרתה בעבר, ומספרי חברה
  חתוכים ב-`effective_date` אינם מספרי החברה — על מסך בלתי-הפיך. (2)
  **`company_event_id` נטבע ב-payload** (append-only ⇒ אין דיעבד);
  **`corrects_event_id` נמחק מההיקף** — האדמין בוחר מנה ולא אירוע. (3) **שני
  פריטי ההיקף תוקנו** (403→404, שלמות מניות) ולא נרשמו כחוב.
- **חמש הכרעות v1.2.0**, מנומקות במלואן ב-`docs/spec/v1.2.0.md` §5: המניות
  הנרכשות **נמחקות** (retired), לא מניות אוצר · **כמות בלבד, בלי מחיר** ·
  פועל על **כל** בעל מניות + `seed_data` מזריע טבלת הון · **אפס חישוב מס**
  (`NOT_COMPUTED`, לעולם לא `0.0` — אין בסיס עלות ואין מסלול מס במודל) ·
  **ה5:** האדמין בוחר מנה במפורש; FIFO/פרו-ראטה הוא כלל עסקי לא מאומת.

- **`FEATURE_SPEC.md` מוזמנה מחדש (13/08/2026)** — הועדפו פיצ'רים גלויים-ללקוח.
  תוכן הגרסאות לא השתנה, רק הסדר: הערכות שווי 1.1.0→**1.4.0** · דוחות/BI
  1.2.0→**1.1.0** · קשיחות/RBAC 1.3.0→**1.5.0** · אירועי חברה 1.4.0→**1.2.0**
  · מיסוי רב-מדינתי 1.5.0→**1.6.0** · אינטגרציות 1.6.0→**1.3.0**.
- **חוב האבטחה החי לא יחולץ כ-patch מוקדם** (CORS, סיסמת ברירת מחדל,
  session-ים, PBKDF2 100K, טוקן 30 יום בלי rotation). נשאר בקבוצת v1.5.0 עד סוף
  התור. הנימוק: שום גרסה לא יצאה ללקוח אמיתי — טסטבד/תרגול בלבד. ההצעה לא
  אמורה לעלות שוב.
- **"אישור קבלה", לא "חתימה"** (v0.9.0). למערכת אין אימות זהות, הצפנה או גורם
  שלישי. המילים `signature`/"חתימה" לא מופיעות באף שדה או תווית — רק בהצהרות
  "אינו חתימה".
- **דחייה בלי שדה סיבה** — `DECLINED` אינו נושא סיבה. נדחה במפורש.
- **גרסה מיושנת לא עוברת שום מעבר מצב**, גם לא אישור — נאכף בשרת
  (`assert_is_current_version`), לא רק במסך.
- **הנייר הישן תמיד נשמר, ותמיד אפשר להוציא נייר חדש.** טריגר ההקפאה מגן על
  *תוכן* האישור בלבד ומאפשר ל-`is_latest` להשתנות.
- **אישור `ExerciseRequest` לא יוצר `ShareIssuance`/`Shareholder`** — זה מה
  שמונע ספירה כפולה בנוסחת ה-fully-diluted. תלויים בו שני הפריטים תחת
  "נסקרו ונדחו במפורש" למטה.

**לקח שחוזר:** כשמוסיפים טבלה חדשה עם `company_id` — `company_scope.TABLE_REGISTRY`/
`SPECIAL_CASED_TABLES` והאינווריאנט
`test_every_company_scoped_table_is_registered_or_explicitly_special_cased`
קיימים בדיוק בשביל זה. הוסף לטבלה הרלוונטית ותן ל-`pytest` להצביע על מה שנשכח.

## חוב פתוח

**מסקירת הפרויקט (14/08/2026):**

- ⬜ **א. כל שדה כספי הוא `Float`, אפס `Numeric`/`Decimal`** (17 עמודות
  ב-`models.py`; ה-CHECK של הפול הוא שוויון צף מדויק). לטנטי ולא באג חי — אין
  דריפט ואין ערך לא-שלם היום — אבל תלוי בכך שכמויות המניות יישארו שלמות לנצח.
  **תוזמן 16/08/2026 כגרסה משלו: `v1.2.3` ב-`FEATURE_SPEC.md`** (שם הנימוק
  המלא), יחד עם פיצול המניות שחסום מאחוריו.
- ✅ **ב, ג, ד — נסגרו ב-v1.1.1.** ראו `docs/handoff/v1.1.1.md`.

**נפתח ב-v1.2.0:**

- ✅ **בדיקת קישור העובד ב-`create_shareholder` — נסגרה בצעד 12ב.** הייתה
  `403` נבדל (אורקל קיום עובד), עכשיו **404 אחיד** עם גוף זהה בשני הענפים.
  ההכרעה מ-15/08 ("החלטת היקף מודעת, נשאר חוב") **התהפכה** ב-17/08 באישור
  המשתתף: התיקון הוא שורה אחת, והבדיקה הקיימת חוזקה ולא הוחלשה.
- ⬜ **שקילות ה-replay מובטחת רק לשורות שנוצרו דרך `create_share_issuance`**
  (`import_.py` כותב `LedgerEvent` ישירות; `backfill_ledger.py` אינו פולט
  `SHARE_ISSUANCE_ESTABLISHED` כלל). ב-DB החי **0 שורות כאלה** וצעד 13 כותב את
  כולן דרך `seed_cap_table`: **תלות בסדר שחרור, לא באג.** סיכון 13 ב-QA.

**נותר פתוח מתוך פריטי v1.1.1:**

- ⬜ **`AuditLog.entity_id.in_(...)`** נשאר `IN(...)` בשתי נקודות
  (`reports.py`, `export.py`) — ל-`AuditLog` אין `company_id` להצטרף אליו,
  והקבוצות מגיעות מ-`scope` בזיכרון. `employee_ids` הוא 260 היום. תקרת
  המשתנים כאן היא 32,766, כלומר רחוק — לטנטי בקנה מידה בלבד.
- ⬜ **3,449 קבצים ב-`export_store` ו-106 ב-`document_store` לא נמחקו** —
  לפי הכרעת המשתתף. כולם יתומים (אפס הפניות ב-DB). `python -m
  tools.sweep_orphan_files` לדוח, `--delete` למחיקה.
- ⬜ **~25 אתרי `onclick="fn('${id}')"` בשלושת הפורטלים** — מחרוזת JS בתוך
  אטריביוט, ההקשר היחיד ש-`escapeHtml` אינו מכסה. `:1019` תוקן ב-v1.1.1 בהסרת
  ההקשר (`data-*` + map); השאר לא. לא נוצל היום (מזהי שרת), אבל **האינווריאנט
  `_RAW_ERROR_INTERPOLATION` אינו רואה את זה** — הוא מכוון ל-`err.message` בלבד.
- ⬜ **`price_id` הוא UUID4, ולכן ל-`stock_prices_history` אין "מי נכתב אחרון".**
  ה-tie-break ב-`_PriceLookup` דטרמיניסטי אך שרירותי בתוך תאריך. אם יידרש
  "המחיר המעודכן גובר", זה מחייב עמודת מועד-כתיבה חדשה — הכרעה לגרסה.
- ⬜ **אינדקס `(company_id, price_date)`** על `stock_prices_history` — נשקל
  ונדחה ב-v1.1.1: אחרי `_PriceLookup` הוא לא קונה כלום מדיד, ועולה מיגרציה.
  רלוונטי רק אם היסטוריית המחירים תגדל בסדר גודל.

**נסקרו ונדחו במפורש — לא נצברו בשקט:**

- ⬜ **פורמט תאריכים מקונפג פר-משתמש** (הועלה 15/08/2026, **נדחה על ידי
  המשתתף באותו יום**). שדות `<input type="date">` כבר עוקבים אחרי הלוקאל של
  מערכת ההפעלה של המשתמש — כלומר הם *כבר* פר-משתמש, ומה שנראה כמו `mm/dd/yyyy`
  קבוע הוא פשוט דפדפן על `en-US`. דריסה מהאפליקציה מחייבת החלפת הבורר הנייטיבי
  ומאבדת מקלדת/מובייל/נגישות. **ההכרעה: לא לשנות דבר**, כולל את ~6 המקומות
  שמרנדרים ISO גולמי. אין לחזור לזה בלי בקשה מפורשת חדשה.

- ⬜ **ניתוק בין מימוש אופציות לטבלת ההון.** אין היום מסך או צורך מאומת
  ל"מי באמת מחזיק מניות כולל עובדים שמימשו", ומימוש נכון דורש סוג אירוע ledger
  חדש + עמודת מעקב + הכרעה על נתונים היסטוריים. סיכון 8 ב-`docs/qa/v1.0.0.md`.
- ✅ **סיכון 3 נסגר ב-v1.2.0 (צעדים 2-5).** `ShareIssuance` נושא עכשיו סוג אירוע
  שני (`SHARE_ISSUANCE_ADJUSTED`), ו-`compute_cap_table_snapshot` עבר ל-replay
  מלא באותה נגיעה — בדיוק כפי שהסעיף הזה דרש. השומר הקבוע הוא בדיקת השקילות
  ב-`tests/test_cap_table.py`, שמחזיקה עותק **מוקפא** של אלגוריתם העמודה הישן
  ואין לעדכן אותו כשקוד הייצור משתנה.

**סיכוני דומיין-מס פתוחים (לא באגים — הכרעות שלא התקבלו):**

- ⬜ **עוגן 102 אומת ברמת מקור משני בלבד** — שכפול הפקודה במאגר חקיקה מסחרי +
  שלושה מקורות מקצועיים בלתי-תלויים; **נוסח ראשוני לא נפתח.** טעון אישור יועץ
  מס/רו"ח לפני שהפלט משמש לדיווח אמיתי.
- ⬜ **`TRUSTEE_HOLDING_MONTHS = 24` הוא קבוע גלובלי בלי `effective_start_date`**,
  בעוד הכלל תלוי-תאריך: לפני תיקון 147 (01/01/2006) הנוסח היה "24 חודשים
  **מתום שנת המס**". מענק היסטורי כזה יחושב היום בכלל הלא נכון. גם מסלול הכנסת
  עבודה (12 חודשים) אינו ממודל.
- ⬜ **הפקדה באיחור ניכר — דינה לא הוכרע.** שתי פרשנויות אפשריות ואף אחת לא
  אומתה. חלונות ההפקדה המנהליים (45/90 יום מהחלטת דירקטוריון) **חסומים
  ליישום**: אין במודל מועד החלטת דירקטוריון, והחלונות נמדדים ממנו ולא
  מ-`grant_date`. מענק כזה מקבל היום תאריך מחושב כרגיל — **וזו אינה הכרעה
  שהתקבלה.**
- ⬜ **אזור זמן עסקי יחיד** — `Asia/Jerusalem` קבוע (עם עקיפה ב-env). לתוכנית
  אמריקאית באותה מערכת אין שעון משלה. **מוסכם וידוע**, כל עוד המערכת מודלת 102.
- ⬜ **ההפקעה כותבת בתוך נתיב GET** (`_documents_out` מריץ `db.commit()`).
  המעבר חד-כיווני ואידמפוטנטי, אבל כל שינוי אחר באותו סשן ייכנס ל-commit הזה.

**סיכוני v1.1.0** (1-9, ב-`docs/qa/v1.1.0.md`) — הבולט שעדיין דורש הכרעה:
`compensation-expense` = 0 על הדאטה הזרוע, כי אין מחיר מניה שקודם למענק
(סיכון 6 — שקול שורת מחיר מוקדמת ב-`seed_data`).

---

## איפה יושב מה

| | |
|---|---|
| כללי הפרויקט | `CLAUDE.md` |
| מפת הקוד | `ARCHITECTURE.md` |
| מודל הסוכנים והברנצ'ים | `AGENT_WORKFLOW.md` |
| דפוסי כשל P1-P6 + הכנת סביבה | `QA_TESTBOOK.md` (אינדקס) |
| מקרי בדיקה לכל גרסה | `docs/qa/<גרסה>.md` |
| **היסטוריית HANDOFF פר-גרסה** | **`docs/handoff/<גרסה>.md` — ארכיון, לא לקריאה בשגרה** |
| אינווריאנטים של הריפו | `tests/test_project_invariants.py` |
| המפרט והיעדים | `FEATURE_SPEC.md`, `GOAL.md` |

## איך סוגרים שיחה

**בסיום כל צעד:** `python tools/context_check.py`. הוא מכריע אם ממשיכים כאן
(🟢) או סוגרים (🟡/🔴) — **לא שואלים את המשתתף.**

1. `docs/qa/<גרסה>.md` מעודכן (Definition of done ב-`CLAUDE.md`).
2. `python -m pytest` עובר.
3. **הקובץ הזה מעודכן** — מצב, צעד הבא, החלטות פתוחות, חוב.
   **כשגרסה נסגרת: הבלוק שלה עובר ל-`docs/handoff/<גרסה>.md`, לא נצבר כאן.**
4. commit + push.
5. שיחה חדשה: `+` ליד `ESOP_test`, שם לפי גרסה. **לא ממשיכים שיחה בין גרסאות.**
