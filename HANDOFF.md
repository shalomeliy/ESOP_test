# HANDOFF — מצב העבודה הנוכחי

**מה זה:** נקודת הכניסה לכל שיחה חדשה. שיחה נסגרת → הקובץ הזה מתעדכן → השיחה הבאה
קוראת אותו ויודעת בדיוק איפה עצרנו. **הידע חי בקבצים, לא בשיחה.**

**למה זה קיים:** סשן אחד הגיע ל-79M טוקנים ו-1,031 תורים כי שלוש גרסאות נבנו בו
בלי לסגור. כל תור קורא מחדש את כל מה שנכתב לפניו, כך שהעלות גדלה עם אורך השיחה
ולא עם קושי המשימה. `CLAUDE.md` שורה 57 כבר אמרה `/clear between unrelated
exercises` — הקובץ הזה הוא מה שהופך את הכלל לבר-ביצוע: יש לאן להעביר את ההקשר.

עדכון אחרון: **2026-08-09**

> **ה-DB החי הועלה ל-head ב-09/08/2026.** הוא היה שלוש מיגרציות מאחור ובלי
> טבלת `documents` — כלומר כל v0.9.0 לא היה קיים בו. גיבוי לפני:
> `db_backups/esop_database.20260809-030922.db` (מחוץ ל-git, מכיל נתונים
> אמיתיים). אחרי: תקין, 4 טריגרים, ספירות זהות, אפס דריפט מול המודלים.

---

## איפה אנחנו

| | |
|---|---|
| גרסה | **v0.9.0 — סגורה.** מוזגה ל-`main`, מתויגת `v0.9.0` |
| שלב | **3 מתוך 3 הושלמו** — שלושת מסכי המסמכים בנויים ומאומתים |
| `VERSION` | `0.9.0` ✅ |
| בדיקות | `224 passed` |
| git | `main` מסונכרן; תגים לכל הגרסאות: v0.3.0, v0.4.0, v0.5.0, v0.5.1, v0.6.0, v0.7.0, v0.9.0 |

## הצעד הבא — הוכרע (09/08/2026)

**v0.9.1 — ייצוא מלא וניידות נתונים**, קריטריון 7 ב-`GOAL.md`.

- **המספר 0.8.0 מת.** הגרסה הזו תוכננה כ-0.8.0, דולגה, ומעולם לא נבנתה. היא
  מוספרה מחדש ל-0.9.1 כי מספר נמוך מ-`VERSION` הנוכחית היה נקרא בדיעבד כאילו
  נבנה לפני v0.9.0. `FEATURE_SPEC.md` עודכן: הסעיף הועבר למקומו לפי הסדר.
- **תיקון החוב נבנה בתוך v0.9.1 וקודם לייצוא** — לא כגרסה נפרדת. קובץ QA אחד
  (`docs/qa/v0.9.1.md`) מכסה את שניהם.
- **v1.0.0** (טבלת הון) נדחית לאחר מכן.

**למה החוב קודם ולא אחרי:** הייצוא מוציא את `recorded_at` של יומן האירועים
לקובץ שהלקוח לוקח למערכת אחרת. חותמת naive הופכת תשובה שגויה על מסך — שמישהו
עוד מרענן — לארטיפקט קבוע שאיש לא יבדוק מולנו שוב. בנוסף, כל שורת קוד חדשה
שכותבת חותמת זמן מגדילה את שטח הפנים של התיקון.

## הכרעות שהתקבלו ב-v0.9.0 (לא לפתוח מחדש)

- **"אישור קבלה", לא "חתימה".** למערכת אין אימות זהות, הצפנה או גורם שלישי.
  המילים `signature`/`signed`/"חתימה" לא מופיעות באף שדה, כפתור או תווית —
  רק בהצהרות "אינו חתימה". `FEATURE_SPEC.md` תוקן בהתאם (הוא הבטיח "חתימה
  דיגיטלית" עד v0.9.0).
- **דחייה בלי שדה סיבה.** `DECLINED` אינו נושא סיבה. נדחה במפורש; רשום כסיכון 10.
- **גרסה מיושנת לא עוברת שום מעבר מצב**, גם לא אישור — נאכף בשרת
  (`assert_is_current_version`), לא רק במסך.
- **הנייר הישן תמיד נשמר, ותמיד אפשר להוציא נייר חדש.** טריגר ההקפאה מגן על
  *תוכן* האישור בלבד ומאפשר `is_latest` להשתנות (מיגרציה `b7c4d1e9f2a3`).

## חוב פתוח

**סיכונים אמיתיים — לא ניקיון:**

- **`conftest.py:76` בונה סכימה ב-`create_all`, שאינו מייצר טריגרים.** כל
  הסוויטה פרט ל-`tests/test_document_triggers.py` רצה בלי אף טריגר — וכך שרד
  באג 500 שלם. כל אינווריאנט חדש שנאכף בטריגר חייב בדיקה בקובץ הייעודי.
  ובאותו קובץ, `conftest.py:100`: `SAWarning: transaction already deassociated
  from connection` — ליקוי שני באותו harness. (סיכון 15)

- **`datetime.utcnow()` בכל הפרויקט — 32 אזהרות ב-`python -m pytest`.**
  מחזיר datetime **naive**. `utcnow()` מסומן להסרה, והפרויקט כבר על Python 3.14.6.

  **שלוש תיקונים שנמדדו ב-09/08/2026 — קראו אותם לפני שמחפשים כשלים:**

  1. **המספר 2,850 שנרשם כאן קודם היה סך *כל* אזהרות הסוויטה.** האזהרות של
     `utcnow` הן **32**. זה לא מקטין את החומרה, אבל העבודה קטנה בהרבה ממה
     שהמספר שידר.
  2. **"שני naive ממקורות שונים" לא מתקיים בפרויקט הזה.** אין ולו
     `datetime.now()` אחד ב-`backend/` (נבדק: 0 מופעים) — כל החותמות מגיעות
     מ-`utcnow()`, כלומר כולן UTC וההשוואות *ביניהן* עקביות. זה לא אומר
     שהבעיה לא קיימת; זה אומר שהיא **לא שם**, ומי שיחפש דריפט פנימי לא
     ימצא כלום ויסיק בטעות שהכל תקין.
  3. **"naive מול aware זורק `TypeError`" נכון בפייתון, ולא נכון דרך SQL.**
     בנתיב SQLAlchemy/SQLite ה-offset **נמחק בשקט** ואין שום חריגה. מי
     שמצפה לקריסה יקבל שקט.

  **הכשל האמיתי יושב בגבול ה-API, וזה המקרה שהודגם בפועל:**
  `knowledge_date` ב-[`routes.py:651`](backend/app/api/routes.py:651) הוא פרמטר
  query, ו-FastAPI מפרסר `?knowledge_date=2026-08-09T12:00:00+03:00` ל-datetime
  **aware**. הוא מושווה ב-[`ledger.py:133`](backend/app/services/ledger.py:133)
  מול `recorded_at` ה-naive, וה-dialect משמיט את ה-offset:

  ```
  event recorded_at : 2026-08-09 10:00 UTC
  cutoff 12:00+03:00 (= 09:00 UTC)
    correct: excluded | system: included  -> SILENTLY WRONG
  ```

  האירוע נוצר **אחרי** נקודת החתך, והמערכת מחזירה אותו כאילו כבר ידעה עליו.
  כלומר `TypeDecorator` על העמודות הוא תנאי הכרחי ולא מספיק: **כל `datetime`
  שנכנס מהלקוח חייב נרמול מפורש ל-UTC בגבול ה-API**, ובדיקה שנופלת בדיוק על
  המקרה למעלה. זה מוסיף גבול רביעי לתיקון (auth · עמודות · ledger/routes ·
  **גבול ה-API**).

  **בקוד הייצור, לפי סדר חומרה:**

  | מקום | מה נשען על זה |
  |---|---|
  | `backend/app/services/ledger.py:81` | **`recorded_at` — ציר "מה המערכת ידעה ומתי" של ה-Ledger הבי-טמפורלי (v0.6.0)** |
  | `backend/app/api/routes.py:156` | `req.reviewed_at` — חותמת אישור/דחייה של בקשת מימוש |
  | `backend/app/api/routes.py:1263` | `now` בנתיב המסמכים |
  | `backend/app/auth.py:31, 39` | `locked_until` — **חלון נעילת חשבון** אחרי כשלונות התחברות |
  | `backend/app/auth.py:54, 79, 92` | תפוגת סשן |

  **הסיכון החמור הוא `ledger.py:81`.** שחזור בי-טמפורלי ("מה היה נכון בתאריך X
  לפי מה שידענו בתאריך Y") נשען כולו על השוואות `recorded_at`. naive שם שובר
  את זה **בשקט** — בלי חריגה, רק תשובה שגויה על שאלה היסטורית. זהו **P6**
  (נאמנות טיפוסים בין כתיבה לקריאה) בקנה מידה של פרויקט.

  **הטבלה למעלה הייתה חלקית.** המיפוי המלא הוא 18 מופעים בחמישה קבצים, ומתוכם
  **9 הם `default=datetime.utcnow` על עמודות** ב-`models.py` (שורות 29, 49, 135,
  225, 240, 255, 268, 341, 485) — כולל `LedgerEvent.occurred_at`. אלה לא הופיעו
  ברשימה הקודמת, והם הרוב. בנוסף `backfill_ledger.py:151`.

  **⚠️ התיקון שנרשם כאן קודם היה שגוי — אומת ונשלל ב-09/08/2026.** נרשם
  "`datetime.now(datetime.UTC)` + עמודות aware". ב-SQLite אין ל-`DateTime` רכיב
  אזור זמן, ו-SQLAlchemy **משמיט את ה-tzinfo בשקט בלי להמיר ל-UTC**.
  `DateTime(timezone=True)` אינו משנה דבר. נמדד:

  | נכתב | נשמר | חוזר |
  |---|---|---|
  | `12:00+03:00` (=09:00 UTC) | `'2026-08-09 12:00:00'` | naive `12:00` |
  | `12:00+00:00` | `'2026-08-09 12:00:00'` | naive `12:00` |

  כלומר "עמודות aware" היה **מכניס שגיאת 3 שעות שקטה** ל-`recorded_at` — בדיוק
  הכשל שהוא נועד למנוע. התיקון הנכון: `TypeDecorator` שממיר ל-UTC בכתיבה
  (ודוחה naive במפורש, במקום לנחש) ומחזיר aware בקריאה, כך שהנרמול יושב בשכבה
  אחת ולא בכל אתר קריאה. במיגרציה אחת מתואמת — לא קובץ-קובץ, אחרת ייווצר בדיוק
  המצב של naive מול aware באותה השוואה.

  → `security-engineer` (auth) + `database-engineer` (העמודות וה-TypeDecorator)
  + `backend-engineer` (ledger/routes). `tax-domain-expert` לא נדרש; אין כאן כלל מס.


**חוב רגיל:**

- **`TaxCalculationResult` (`backend/app/services/tax_engine.py:45`) — אין כיסוי
  בדיקות.** אובייקט שנושא `tax_amount` ו-`effective_rate` בלי טסט. מתחבר ל-P4.
- `routes.py` ו-`admin_portal/index_manage.html` הם מונוליטים, וגם נקודות
  ההתנגשות של סוכנים במקביל (`AGENT_WORKFLOW.md` שורות 67-79).
- `EXPIRED` מוגדר במכונת המצבים ואין קוד שמייצר אותו — מדיניות תפוגה טרם הוכרעה.
  אין להציג ספירת ימים לתפוגה עד שתתקבל. (סיכון 8)
- `db.query(...).get()` ב-`tests/test_ledger_replay.py:282, 286` — `LegacyAPIWarning`,
  legacy מאז SQLAlchemy 2.0. `Session.get()` במקומו. בבדיקות בלבד, זניח.
- ~8 קלאסים ב-`backend/app/schemas.py` משתמשים ב-`class Config` של Pydantic,
  שסומן להסרה ב-Pydantic V3 (`EmployeeOut`, `CompanyOut`, `GrantOut`, `PoolOut`,
  `AuditLogOut`, `DocumentOut` ואחרים). שבירה עתידית מובטחת, לא דחופה —
  `ConfigDict` במקומו.
- קישור מסמכים לישות כמסך ("כל המסמכים של העובד הזה") — היה בתכנון v0.9.0, לא נבנה.

---

## איפה יושב מה

| | |
|---|---|
| כללי הפרויקט | `CLAUDE.md` |
| מודל הסוכנים והברנצ'ים | `AGENT_WORKFLOW.md` |
| דפוסי כשל P1-P6 + הכנת סביבה | `QA_TESTBOOK.md` (אינדקס) |
| מקרי בדיקה לכל גרסה | `docs/qa/<גרסה>.md` |
| אינווריאנטים של הריפו | `tests/test_project_invariants.py` |
| המפרט והיעדים | `FEATURE_SPEC.md`, `GOAL.md` |

## איך סוגרים שיחה

1. `docs/qa/<גרסה>.md` מעודכן (Definition of done ב-`CLAUDE.md`).
2. `python -m pytest` עובר.
3. **הקובץ הזה מעודכן** — מצב, צעד הבא, החלטות פתוחות, חוב.
4. commit + push.
5. שיחה חדשה: `+` ליד `ESOP_test`, שם לפי גרסה. **לא ממשיכים שיחה בין גרסאות.**
