# v0.9.1 — ייצוא וניידות נתונים (בבנייה)

> **שתי שלבים.** שלב א (נאמנות חותמות זמן + סגירת חוב) **הושלם**; שלב ב (הייצוא
> עצמו) טרם נבנה. הגרסה נסגרת רק כששניהם בפנים.
>
> הגרסה מוספרה מ-0.8.0 ל-0.9.1 ב-09/08/2026. ראו `FEATURE_SPEC.md`.

## למה תיקון החוב קודם לייצוא

הייצוא מוציא את `recorded_at` של יומן האירועים לקובץ שהלקוח לוקח למערכת אחרת.
חותמת שגויה על מסך היא טעות שמישהו עוד מרענן; אותה חותמת בקובץ ייצוא היא ארטיפקט
קבוע שאיש לא יבדוק מולנו שוב.

---

## (א) מקרי בדיקה — שלב א

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-091-01` | `GET /admin/ledger/OptionPool/{id}/as-of?knowledge_date=<12:00+03:00>` על אירוע שנרשם ב-10:00 UTC | אדמין החברה | האירוע **לא** מוחזר. החתך הוא 09:00 UTC, כלומר לפני האירוע | `test_knowledge_cutoff_with_offset_excludes_an_event_recorded_later` |
| `QA-091-02` | אותו חתך, אבל `12:00+00:00` | אדמין החברה | האירוע **כן** מוחזר — הצד השני של אותו מטבע | `test_the_same_wall_clock_in_utc_does_include_it` |
| `QA-091-03` | כתיבת חותמת naive לעמודת `UtcDateTime` | — | נדחה ב-`ValueError`. לא מנוחש, לא נשמר | `test_a_naive_timestamp_is_rejected_and_not_guessed` |
| `QA-091-04` | שמירת `12:00+03:00` וקריאה חוזרת | — | חוזר `09:00 UTC` — אותו רגע בזמן, לא אותו שעון קיר | `test_a_non_utc_timestamp_is_converted_and_not_truncated` |
| `QA-091-05` | קריאת כל חותמת מה-DB | — | `tzinfo` אינו `None`, היסט 0 | `test_reads_come_back_aware` |
| `QA-091-06` | `knowledge_date` בלי היסט מגיע מהלקוח | — | מפורש כ-UTC בגבול ה-API בלבד | `test_ensure_utc_reads_naive_client_input_as_utc` |
| `QA-091-07` | 5 כשלונות התחברות, ואז **שליפה מחדש מה-DB** | משתמש כלשהו | `locked_until` מאוכלס ו-`failed_login_attempts=5` | `test_account_lockout_is_actually_persisted` |
| `QA-091-08` | `python -m pytest` | הסוויטה | הסכימה נבנית ב-`alembic upgrade head` ו-4 הטריגרים פעילים | `create_schema` נכשל אם אין טריגרים |
| `QA-091-09` | `datetime.utcnow(` בכל `backend/` | הריפו | 0 מופעים (למעט התיעוד ב-`types.py`) | `test_no_naive_utcnow_in_backend` |
| `QA-091-10` | `date.today()` בכל `backend/` | הריפו | 0 מופעים. **אין חריגים** — החריג של `termination_date` נסגר בתיקון ח1/ח2 | `test_no_host_local_date_today_in_backend` |
| `QA-091-11` | השמה ל-`grant_date`/`trustee_deposit_date`/`exercise_date` משעון | הריפו | 0 — תאריך מס מגיע מהמסמך | `test_the_clock_is_never_the_source_of_a_tax_date` |
| `QA-091-12` | הרצת `database/init_scheme.sql` והשוואה ל-`Base.metadata` | הריפו | 19 טבלאות, זהות מלאה בשני הכיוונים | `test_init_scheme_sql_documents_every_table_in_the_models` |
| `QA-091-13` | טריגרים ב-`init_scheme.sql` | הריפו | כל 4 מתועדים | `test_init_scheme_sql_documents_the_triggers` |
| `QA-091-14` | חישוב מס שטוח למימוש ב-2023 | — | `pack_id` הוא של גרסת 2020 ו-`source_url` מאוכלס | `test_flat_result_carries_the_source_of_the_version_actually_used` |
| `QA-091-15` | אותו חישוב למימוש ב-2024 | — | `pack_id` מתחלף לגרסת 2024, לא רק השיעור | `test_the_pack_id_moves_to_the_newer_version_not_only_the_rate` |

### תיקון ח1/ח2 — השעון העסקי (09/08/2026)

שלב א העביר גבולות מזכים ל-UTC. הכיוון היה נכון (תאריך המארח אינו דטרמיניסטי)
אבל היעד שגוי: ישראל *לפני* UTC. הוחלף ב-`business_today()` — אזור זמן עסקי
**מפורש**, לא של המארח. שיטת האימות: כל בדיקת HTTP כאן מחליפה את
`routes.business_today` בלבד, ולכן חזרה ל-`system_today_utc()` באתר הקריאה
מפילה אותה. זה בדיוק הכיסוי שחסר קודם — 11 האתרים היו מכוסים ב-grep בלבד.

| מס' | מה נבדק | מי | תוצאה צפויה | הבדיקה |
|---|---|---|---|---|
| `QA-091-16` | 30/08/2025 22:00Z (= 01:00 בירושלים ב-31/08) | — | `system_today_utc()` = 30/08, `business_today()` = 31/08 | `test_utc_clock_reports_yesterday_at_01_00_jerusalem` |
| `QA-091-17` | 31/12/2026 22:30Z (= 00:30 בירושלים ב-01/01/2027) | — | UTC נותן שנת מס 2026; העסקי נותן 01/01/2027 | `test_utc_clock_crosses_a_tax_year_backwards` |
| `QA-091-18` | `POST /employee/exercise-requests` **ביום הדדליין עצמו** | עובד שעזב | `200` — התנאי הוא `<=` | `test_window_is_open_on_the_deadline_day_itself` |
| `QA-091-19` | אותה בקשה **יום אחרי** הדדליין | עובד שעזב | `400`, וההודעה נוקבת בדדליין | `test_window_is_closed_the_day_after_the_deadline` |
| `QA-091-20` | `effective_date` של `EXERCISE_REQUEST_SUBMITTED` | עובד | יום העסקים, לא תאריך ה-UTC. `recorded_at` נשאר UTC | `test_ledger_effective_date_is_the_business_day` |
| `QA-091-21` | תאריך-לפי-UTC בכל `backend/app/` — בשלושה איותים (`system_today_utc(`, `utcnow().date()`, `datetime.now(timezone.utc).date()`) | הריפו | 0 מופעים — שכבת האפליקציה רצה על השעון העסקי | `test_app_layer_runs_on_the_business_clock_not_utc` |
| `QA-091-22` | חותמת `UtcDateTime` **מאוחסנת** שנקראת כיום עסקי | — | `30/08 22:00Z` → `.date()` נותן 30/08, `business_date_of` נותן 31/08 | `test_a_stored_utc_timestamp_is_read_as_its_business_day` |
| `QA-091-23` | בקשה שהוגשה 31/08 ב-01:00 בירושלים, נבדקת ב-06/09 מול סף 7 ימים | — | **אין** התראה (ההמתנה 6 ימים). ב-07/09 כן | `test_pending_too_long_does_not_count_an_extra_day_of_waiting` |

**אימות שהבדיקות אינן ריקות:** ח1/ח2 הוחזרו זמנית לקוד והסוויטה הורצה מחדש —
`QA-091-18`, `QA-091-20` ו-`QA-091-21` נפלו. `QA-091-19` עבר גם עם הרגרסיה
(התאריך האמיתי רחוק מהדדליין ממילא), ולכן **המשקל על `QA-091-18`** — הוא הצד
של הגבול שמבחין בין השעונים. השניים נשמרים כזוג כדי שהגבול יופיע בשלמותו.
`QA-091-23` אומת באותה שיטה בנפרד: החזרת `.date()` ל-`notifications.py:186`
מפילה אותו ורק אותו.

### מה הסקירה העצמאית מצאה (09/08/2026)

`PASS`, **אפס ממצאים חוסמים**. ארבע הערות; שלוש טופלו כאן ואחת תועדה:

1. **ערבוב שעונים ב-`_rule_request_pending_too_long`** — `requested_at` הוא UTC
   ו-`today` הפך לעסקי, כלומר הרחבת ההיקף הכניסה גבול חדש. **תוקן**
   (`business_date_of`), עם `QA-091-22`+`QA-091-23`.
2. **`QA-091-19` חלול** — אמת, ומתועד למעלה. לא שונה.
3. **שורות `ledger_events` היסטוריות על השעון הישן** — **נרשם** כסיכון 4.
4. **האינווריאנט היה טקסטואלי מדי** — `utcnow().date()` היה חומק. **חוזק**
   לשלושה איותים (`QA-091-21`).

### החישוב הידני שמאחורי QA-091-01

```
אירוע נרשם     : 2026-08-09 10:00:00+00:00
חתך ידיעה      : 2026-08-09 12:00:00+03:00  (= 09:00:00 UTC)
הנכון          : 0 אירועים — האירוע נוצר אחרי החתך
DateTime ישן   : 1  ← שגוי, ובלי שום חריגה
UtcDateTime    : 0  ← נכון
```

נמדד ישירות מול שתי עמודות באותו DB. **הכשל הישן לא זרק חריגה** — ולכן בדיקה
שרק מוודאת "לא קרס" הייתה עוברת גם לפניו.

---

## (ב) אזורי סיכון

חולשות **בקוד שקיים בפועל** בלבד.

| # | הסיכון | דפוס | איפה בקוד |
|---|---|---|---|
| 1 | `termination_date` **עדיין נגזר משעון** ברגע שאדמין לוחץ. השעון תוקן (`business_today()`, ולכן גם אותו שעון כמו הדדליין שנגזר ממנו) אבל הפגם העמוק פתוח: תאריך סיום העסקה הוא עובדת HR שלרוב מתרחשת בעבר. **הפתרון הוא קלט מפורש, לא שעון** | P1 | `routes.py:477` |
| 2 | אזור הזמן העסקי הוא קבוע יחיד (`Asia/Jerusalem`, ניתן לעקיפה ב-`ESOP_BUSINESS_TIMEZONE`). לתוכנית אמריקאית באותה מערכת אין כרגע שעון משלה — ארה"ב מאחורי UTC וישראל לפניו, ואין ערך יחיד שנכון לשתיהן. **מוסכם וידוע**, לא תקלה: המערכת מודלת 102 בעיקר | P1 | `types.py` |
| 3 | התיקון תלוי ב-`tzdata`. בלעדיו `ZoneInfo("Asia/Jerusalem")` נכשל ב-Windows — ולכן הכשל **מפורש ב-import** ולא נפילה שקטה ל-UTC, שהייתה מחזירה את הבאג במכונה שבה אי אפשר לראותו. `BUSINESS_TIMEZONE` נפתר פעם אחת ב-import: שינוי ה-env תוך כדי ריצה אינו משפיע | P6 | `types.py`, `requirements.txt` |
| 4 | **שורות `ledger_events` היסטוריות נשארות על השעון הישן** — מה שנכתב בשלב א נגזר מ-UTC, ומה שלפניו מתאריך המארח. `trg_ledger_events_no_update` הופך אותן לבלתי ניתנות לתיקון **בכוונה**, ולכן אין מיגרציה. רלוונטי לכל replay בי-טמפורלי שחוצה גבול שנת מס | P1 | `ledger_events` |
| 5 | `ensure_utc` מפרש naive כ-UTC. זה הגבול היחיד שבו ניחוש מותר, ומנומק: ה-API פלט עד היום naive-UTC. אם פורמט הפלט ישתנה, ההנחה הזו חייבת להיבחן מחדש | P6 | `types.py`, `routes.py` |
| 6 | פורמט ה-JSON היוצא השתנה: `...067634` הפך ל-`...067634Z`. הפורטלים עשו `new Date(...)` על ערך ללא היסט, שנקרא כזמן **מקומי** — כלומר הם הציגו עד היום שעה שגויה בהיסט המארח. התצוגה תשתנה; זה **תיקון**, לא רגרסיה | P6 | `index_manage.html:757`, `index_emp.html:546` |
| 7 | `isoformat()` של חותמת aware מסתיים ב-`+00:00`, וה-`+` ב-query string פירושו רווח. לקוח שישרשר ידנית לכתובת יקבל 422. הפורטל משתמש ב-`URLSearchParams` ולכן תקין | P6 | `index_manage.html:773` |
| 8 | שלב ב טרם נבנה — אין עדיין ייצוא, ייבוא, dry-run או דוח התאמה | — | — |

---

## מה **לא** נכלל בגרסה הזו

- **מדיניות `EXPIRED`** — טרם הוכרעה. אין להציג ספירת ימים לתפוגה עד שתתקבל.
- **פיצול המונוליטים** (`routes.py`, `index_manage.html`) — טרם הוכרע היקף.
- **חלון 102 של 24 חודשים** נמדד מ-`trustee_deposit_date`, בעוד המקורות מתארים
  את התקופה מיום ההקצאה. בנתוני הזרע הפער הוא ~30 יום. **חסום לאימות** מול
  §102(א) לפקודה ותקנות 2003 — יועץ מס ישראלי או נאמן התוכנית. הועלה בבדיקת
  `tax-domain-expert` ב-09/08/2026 והופרד במכוון מהתיקון הזה.
