# v1.0.1 — תיקוני באגים (4 פריטי חוב מ-`HANDOFF.md`)

> גרסת patch, לא פיצ'ר: ארבעה פריטי חוב פתוחים מ-v1.0.0/v0.9.1 נבחרו במפורש
> ע"י המשתתף ("start with debt missions" → "do all of them"). כל אחד עבר
> את תהליך התכנון המלא (`CLAUDE.md` שלב 4: product/architecture/design/qa/
> security-engineer read-only) לפני מימוש. פרטים מלאים ב-`HANDOFF.md`.

## (א) מקרי בדיקה

### פריט 1 — ייצוא/ייבוא לא כלל את טבלת ההון (`docs/qa/v1.0.0.md` סיכון 4, נסגר כאן)

`ShareClass`/`Shareholder`/`ShareIssuance` נוספו ל-`TABLE_REGISTRY`
(`services/export.py`) ולמבני `_TABLE_SPECS`/`_MODEL_BY_TABLE`/`_PK_COLUMN`/
`_FORCE_COMPANY_ID_TABLES`/`_LEDGER_AGGREGATE_CATEGORY`/`_AGGREGATE_TYPE_BY_TABLE`
(`services/import_.py`), ו-`OptionPool.share_class_id` קיבל `fk_check` נאלבילי.

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-101-01` | ייצוא חברה עם סוג מניה, שני בעלי-מניות (אחד מקושר לעובד, אחד חיצוני), והנפקה אחת | אדמין החברה | שלוש הטבלאות מופיעות ב-bundle עם השדות הנכונים | `test_export_includes_cap_table_entities_with_correct_fields` |
| `QA-101-02` | ייצוא פול עם `share_class_id` מוגדר, ופול בלי (`None`) | אדמין החברה | שני הפולים מיוצאים, `share_class_id` נכון בכל אחד | `test_export_carries_option_pool_share_class_id_both_set_and_none` |
| `QA-101-03` | ייבוא `commit()` של bundle עם סוג מניה+בעל-מניות+הנפקה לחברת יעד טרייה | — | שלוש השורות נכתבות, ואירוע `SHARE_ISSUANCE_ESTABLISHED` + `LedgerOwnership` נכונים נוצרים ל-`ShareIssuance` | `test_commit_writes_cap_table_entities_and_the_share_issuance_ledger_event` |
| `QA-101-04` | `dry_run` על שורת `Shareholder` עם `employee_id` שקיים בפועל אך בחברה **אחרת** | — | `ERROR`, לא `SKIP_EXISTING`/`NEW` בשקט | `test_dry_run_rejects_a_shareholder_whose_employee_id_is_outside_the_target_scope` |
| `QA-101-05` | `dry_run` על שורת `ShareIssuance` עם `shareholder_id`/`share_class_id` שלא נמצאים בבאצ' ולא ב-scope היעד | — | `ERROR` | `test_dry_run_rejects_a_share_issuance_with_unresolvable_shareholder_or_share_class` |
| `QA-101-06` | **קריטי לאבטחה**: `commit()` על bundle מזויף/מיושן שבו `company_id` בשורת `shareholders`/`share_issuances` הוא של חברה **אחרת** אמיתית | — | השורה הנכתבת נושאת את `company_id` של חברת **היעד**, לא הערך מהקובץ - `_FORCE_COMPANY_ID_TABLES` מכסה את שלוש הטבלאות | `test_commit_force_overwrites_a_crafted_foreign_company_id_on_shareholders_and_share_issuances` |
| `QA-101-07` | ייבוא פול עם `share_class_id` שכבר קיים ביעד, ופול עם `share_class_id=None` | — | שני הפולים מיובאים נכון | `test_commit_round_trips_option_pool_share_class_id_set_and_none` |
| `QA-101-08` | **נמצא תוך כתיבת הבדיקות, לא אחד מארבעת הפריטים המתוכננים**: ייבוא חברה מלאה ליעד טרי, שבה סוג מניה **חדש בתוך אותו batch** (לא קיים עדיין ביעד) והפול שמפנה אליו | — | מיובא בהצלחה, `pool.share_class_id` נכון - לפני התיקון נכשל ב-`ERROR` שגוי כי `option_pools` עובד לפני `share_classes` ב-`_TABLE_SPECS`/`_MODEL_BY_TABLE` (תוקן: `share_classes` הוזזה לפני `option_pools` בשני המבנים) | `test_a_pool_and_its_new_share_class_in_the_same_batch_imports_cleanly` |

### פריט 2 — `holding_period_end_date` הציג "היום" במקום "לא ידוע" (`HANDOFF.md`)

`_trustee_holding_status(grant, today)` (עוזר חדש ב-`exercise_requests.py`,
לצד `_vested_at`) מחזיר `(False, None)` כש-`trustee_deposit_date` חסר, במקום
לסמוך על `check_trustee_holding_period` שמחזיר `(False, today)` בכוונה
(חוזה נכון של המנוע, אבל שיקרי כשמוצג כמו שהוא). שולב בארבעה נקודות קריאה
+ `ExerciseSimulationResponse.holding_period_end_date` הפך ל-`Optional[date]`.
תיקון תואם ב-`clients/employee_portal/index_emp.html` (שני מקומות לא-מוגנים).

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-101-09` | `GET /trustee/portfolio` על מענק עם `trustee_id` אך בלי `trustee_deposit_date` | נאמן | `holding_period_end_date: null`, `is_trustee_holding_period_met: false` - לא תאריך היום | `test_trustee_portfolio_with_no_deposit_reports_null_holding_period_end_date` |
| `QA-101-10` | אותו endpoint, מענק עם תאריך הפקדה (מקרה עמד ומקרה לא-עמד) | נאמן | `holding_period_end_date` הוא תאריך אמיתי בשני המקרים | `test_trustee_portfolio_with_a_deposit_reports_a_real_end_date_met_and_unmet` |
| `QA-101-11` | `GET /employee/dashboard/{id}` על מענק בלי הפקדה | עובד | `holding_period_end_date: null` (לא המחרוזת `"None"`) | `test_employee_dashboard_with_no_deposit_reports_null_holding_period_end_date` |
| `QA-101-12` | `POST /employee/simulate-exercise` על מענק בלי הפקדה | עובד | `200` (לא `500` מקריסת ולידציית Pydantic), `holding_period_end_date: null` | `test_simulate_exercise_with_no_deposit_returns_200_with_null_holding_period_end_date` |
| `QA-101-13` | אישור בקשת מימוש (`PATCH /admin/exercise-requests/{id}` approve=true) על מענק עם נאמן ובלי הפקדה | אדמין | `400` - עדיין נחסם נכון (`is_met=false` בלי תלות בערך `end_date`), וההודעה **לא** טוענת שהחלון "לא עמד עד היום" | `test_approving_with_no_deposit_is_blocked_without_claiming_todays_date_as_the_deadline` |

### פריט 3 — אין ולידציה על `termination_date` (`HANDOFF.md`, דפוס P3)

`_validate_termination_date(termination_date, hire_date, today)` (עוזר טהור
חדש ב-`employees.py`) דוחה `termination_date < hire_date` ו-`> business_today()`
(**לא** `date.today()`). מוחל **זהה** על שני הנתיבים
(`DELETE /admin/employees/{id}` ו-`PATCH .../status`) - בדיקה פרמטרית אחת
חוצה את שניהם, כדי שלא יוכלו לסחוף שוב (P3, `QA_TESTBOOK.md`).

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-101-14` | `termination_date` = יום לפני `hire_date`, על שני הנתיבים | אדמין | `400` בשניהם, `detail` נוקב `termination_date` | `test_termination_date_boundary_is_enforced_identically_on_both_endpoints[before_hire_date-...]` |
| `QA-101-15` | `termination_date == hire_date` (גבול), על שני הנתיבים | אדמין | `200` בשניהם - יום גיוס=עזיבה מותר | `test_termination_date_boundary_is_enforced_identically_on_both_endpoints[on_hire_date_boundary_allowed-...]` |
| `QA-101-16` | `termination_date == business_today()` (גבול), על שני הנתיבים | אדמין | `200` בשניהם | `test_termination_date_boundary_is_enforced_identically_on_both_endpoints[on_today_boundary_allowed-...]` |
| `QA-101-17` | `termination_date` = מחר, על שני הנתיבים | אדמין | `400` בשניהם | `test_termination_date_boundary_is_enforced_identically_on_both_endpoints[one_day_in_the_future-...]` |
| `QA-101-18` | `termination_date` = בעוד 10 שנים, על שני הנתיבים | אדמין | `400` בשניהם | `test_termination_date_boundary_is_enforced_identically_on_both_endpoints[years_in_the_future-...]` |

*(5 המקרים × 2 נתיבים = 10 פרמטריזציות בפועל תחת פונקציית בדיקה אחת.)*

### פריט 4 — חלון האישור היה קבוע גלובלי יחיד (`docs/qa/v0.9.1.md` סיכון 10)

`Company.acknowledgment_window_days` (עמודה נאלבילית חדשה, `Integer`, `CHECK
> 0`, מיגרציה `137b929afafc`) - `None` אומר "אין override, השתמש ב-30 הגלובלי",
בדיוק כמו `total_authorized_shares`. `deadline_for(sent_at, window_days=None)`
נשאר פונקציה טהורה; `documents.py` פותר את ה-override ומעביר אותו.

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-101-19` | חברה בלי override, שליחת מסמך | אדמין | `expires_at = sent_at + 30 יום` - התנהגות קודמת, ללא שינוי | `test_default_window_stays_thirty_days_when_the_company_has_no_override` |
| `QA-101-20` | `PUT /admin/company {acknowledgment_window_days: 10}`, ואז שליחת מסמך | אדמין | `expires_at = sent_at + 10 יום`, לא 30 | `test_company_override_changes_the_window_for_documents_sent_after_it_is_set` |
| `QA-101-21` | `PUT /admin/company {acknowledgment_window_days: 0}` ו-`-5` | אדמין | `400` נקי בשני המקרים (**לא** `500` מ-`IntegrityError` גולמי על ה-`CHECK`), הערך הקיים (`None`) לא נכתב | `test_put_company_rejects_a_non_positive_acknowledgment_window_days[0]` / `[-5]` |
| `QA-101-22` | מסמך היסטורי עם `expires_at=NULL` (נשלח לפני התכונה), ואז החברה מגדירה override | אדמין/עובד | `expires_at` נשאר `NULL` - אין דדליין רטרואקטיבי | `test_a_document_sent_before_the_feature_existed_is_unaffected_by_a_later_company_override` |
| `QA-101-23` | מסמך נשלח תחת override=10, ואז החברה משנה ל-20 | אדמין | `expires_at` של המסמך הראשון **לא** משתנה; מסמך חדש שנשלח אחר כך מקבל 20 | `test_changing_the_override_after_sending_does_not_change_the_already_stored_expires_at` |
| `QA-101-24` | `deadline_for(sent_at, window_days=10)` על תאריך קרוב לגבול חודש/שנה; `deadline_for(sent_at)` בלי override | — | `sent_at + N days` בדיוק (חשבון timestamp, לא קלנדרי), וברירת המחדל נופלת לקבוע הגלובלי | `test_deadline_for_adds_exact_days_across_a_month_boundary_not_calendar_days` |

### רגרסיה קיימת שעודכנה (עמודה חדשה, לא באג)

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-101-25` | ייצוא חברה בלי `acknowledgment_window_days` שהוזן | אדמין | `tables["companies"][0]["acknowledgment_window_days"] == null` - עמודה חדשה, nullable, מיוצאת גם כשלא הוזנה | `test_export_round_trips_company_and_employee_data` (מתוקן, אותו דפוס כמו `QA-100-37`) |

### אינווריאנטים כלליים (ללא שינוי מכוון, אומתו מריצה בפועל)

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-101-26` | `python -m pytest` מלא | הריפו | `411 passed, 0 failed` (היה `381` בתחילת הגרסה) | הסוויטה המלאה |

---

## (ב) אזורי סיכון

חולשות **בקוד שקיים בפועל** בלבד.

| # | הסיכון | דפוס (P1-P6) | איפה בקוד |
|---|---|---|---|
| 1 | **סגור.** ~~טבלת ההון לא נכללת בייצוא/ייבוא~~ - ראו `docs/qa/v1.0.0.md` סיכון 4. ממומש ונבדק ב-`QA-101-01`..`QA-101-08` | — | `backend/app/services/export.py`, `backend/app/services/import_.py` |
| 2 | **נמצא ותוקן תוך כדי (לא אחד מארבעת הפריטים המתוכננים).** `_TABLE_SPECS`/`_MODEL_BY_TABLE` ב-`import_.py` עיבדו `option_pools` לפני `share_classes` - הגירת חברה מלאה עם סוג מניה חדש *בתוך אותו batch* הייתה נכשלת ב-`ERROR` שגוי. תוקן בהזזת `share_classes` לפני `option_pools`. נעול ב-`QA-101-08` | P3-דמוי (סדר עיבוד, לא ולידציה חסרה) | `backend/app/services/import_.py::_TABLE_SPECS`, `_MODEL_BY_TABLE` |
| 3 | **פתוח, מתועד במפורש (לא חוב שנצבר בשקט).** ייצוא/ייבוא עדיין משתמשים בשני מבנים מקבילים (`export.py::TABLE_REGISTRY` מול `import_.py::_TABLE_SPECS`/`_MODEL_BY_TABLE`) שמקודדים כמעט את אותו מידע (מודל, מפתח ראשי, קטגוריית scope) משתי זוויות - זה מה שאיפשר לפער המקורי (סיכון 1 למעלה) לקרות. איחוד לתיאור-טבלה משותף יחיד נדחה בכוונה מגרסת ה-patch הזו (ראו `HANDOFF.md`) - שינוי אמיתי, לא באג שהוא מתקן, ובלתי-מוצדק להרחיב את ההיקף של תיקון-באגים בשבילו | — | `backend/app/services/export.py`, `backend/app/services/import_.py` |
| 4 | **פתוח, ללא שינוי - ריק בכוונה.** `_assert_request_approvable`'s error message (הנקודה הרביעית ב-פריט 2, `exercise_requests.py`) תוקנה לצד שלוש הנקודות האחרות - אין סיכון נוסף שנפתח כאן, ה-`is_met` היה נכון תמיד (החסימה עצמה מעולם לא הייתה שגויה, רק ניסוח ההודעה) | — | `backend/app/api/exercise_requests.py::_assert_request_approvable` |
| 5 | **`acknowledgment_window_days` הוא `nullable=True` בלי ברירת מחדל מפורשת בטבלה** - בדיוק כמו `total_authorized_shares`. `None` נבדק בקוד במפורש כ"השתמש בקבוע הגלובלי", לא "0 יום" - אותו דפוס P4 שכבר תועד ונבדק ב-v1.0.0, כאן נבדק ב-`QA-101-19` | P4 | `backend/app/models.py::Company.acknowledgment_window_days` |
| 6 | **חוב שלא נסגר בגרסה הזו (מחוץ להיקף במפורש).** תוקף מסמך אישור נשאר קבוע פר-**חברה**, לא פר-סוג-מסמך - `docs/qa/v0.9.1.md` סיכון 10 נסגר רק חלקית. אין צורך מוצהר לגרנולריות עדינה יותר כרגע | — | `backend/app/services/document_status.py` |

---

## מה **לא** נכלל בגרסה הזו

- **RBAC דק יותר מ-`COMPANY_ADMIN`, `seniority_order` בלי `UNIQUE`, `ShareIssuance` בסוג אירוע יחיד** - כל השלושה נשארים פתוחים בדיוק כמו ב-`docs/qa/v1.0.0.md` (סיכונים 3/5/6 שם); לא היו בהיקף ארבעת הפריטים שנבחרו לגרסה הזו.
- **איחוד מבני `TABLE_REGISTRY`/`_TABLE_SPECS`** - ראו סיכון 3 למעלה. שינוי ארכיטקטוני נדחה מכוונה, לא חוב שנצבר בשקט.
- **חלון אישור פר-סוג-מסמך** - ראו סיכון 6 למעלה.
