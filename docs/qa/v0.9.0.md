# v0.9.0 — מסמכים ואישור קבלה פנימי (שלבים 1–2 מתוך 3 — בפיתוח)

**מטרה:** כל מענק מייצר את הנייר שלו בתוך המערכת, עם מעקב אישור-קבלה פנימי כנה.

**הכרעה מרכזית (הוחלטה בתכנון, אחרי fan-out ל-5 מומחים):** *** לא "חתימה
דיגיטלית" ***. למערכת אין אימות זהות, אין הצפנה ואין גורם שלישי מאשר — "חתימה"
הייתה מתחזה לתוקף משפטי שאין לה, בדיוק אותה הפרה כמו כלל מס בדוי (`GOAL.md`
חוק ברזל 1). לכן: **"אישור קבלה" (acknowledgment)** בכל מקום — קוד, API, UI,
וגוף ה-PDF עצמו. המילים `signature`/`signed`/"חתימה" לא מופיעות באף שדה.

**היקף שלב 1:** סכמה + מיגרציה + מנוע PDF + כתב הענקה בלבד + פונקציית הרשאה
משותפת + יצירה/הורדה ל-admin. **שלב 2**: שתי התבניות הנוספות (נספח 102 כדמו
מסומן, אישור הפקדת נאמן), מכונת המצבים המלאה (`DRAFT→SENT→ACKNOWLEDGED/DECLINED/
EXPIRED`, מעבר לא-חוקי = 409), endpoints לשלושת התפקידים, וטריגר DB שמקפיא מסמך
שאושר. שלב 3 (טרם בוצע): שלושת מסכי ה-UI.

**החלטות תכנון נוספות:** מענק בלי לוח הבשלה → כשל מפורש (409), לא PDF חלקי ·
שינוי נתונים אחרי יצירה → גרסה חדשה, הישנה נשמרת · נספח 102 = תבנית דמו מסומנת
(כמו טבלאות המס) · `Employee.national_id` נוסף (טקסט חופשי, בלי ולידציית פורמט).

## (א) מקרי בדיקה

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-090-01` | יצירת כתב הענקה | `POST /admin/documents`, `GRANT_LETTER` | `200`; `status=DRAFT`, `version=1`, `is_latest=true`; קובץ PDF אמיתי נכתב לדיסק בגודל > 0 | `test_generate_grant_letter_succeeds_and_writes_a_real_file` |
| `QA-090-02` | `template_type` לא מוכר | סוג שלא ב-`DOCUMENT_TEMPLATE_TYPES` | `400` | `test_unsupported_template_type_is_rejected` |
| `QA-090-03` | סוג חוקי שטרם מומש | `SECTION_102_APPENDIX` (שלב 2) | `400` מפורש — לא ניסיון ליצור PDF ריק/שגוי בשקט | `test_not_yet_implemented_template_type_is_rejected_not_silently_generated` |
| `QA-090-04` | מענק לא קיים | `grant_id` שאינו קיים | `404` | `test_unknown_grant_returns_404` |
| `QA-090-05` | **P4**: מענק בלי לוח הבשלה | לוח הבשלה נמחק לפני הקריאה | `409` · `"...vesting schedule..."` — כשל מפורש, לא PDF בלי סעיף הבשלה | `test_grant_without_vesting_schedule_fails_loudly_not_partial_pdf` |
| `QA-090-06` | גרסה חדשה, לא דריסה | יצירה פעמיים לאותו מענק+סוג | `version=2`, `is_latest=true`; הראשונה `is_latest=false` והקובץ שלה **עדיין קיים** | `test_regenerating_creates_a_new_version_not_an_overwrite` |
| `QA-090-07` | **P2/IDOR**: יצירה חוצת-חברות | אדמין חברה B על מענק בחברה A | `403` | `test_admin_cannot_generate_document_for_another_companys_grant` |
| `QA-090-08` | **P2/IDOR**: הורדה חוצת-חברות | אדמין חברה B מוריד מסמך של A | `403` | `test_admin_cannot_download_another_companys_document` |
| `QA-090-09` | מסמך לא קיים | `document_id` שאינו קיים | `404` | `test_unknown_document_id_download_returns_404` |
| `QA-090-10` | שלב 1 admin-only | טוקן TRUSTEE ואז EMPLOYEE, יצירה **והורדה** | `403` בכל 4 השילובים | `test_non_admin_roles_cannot_generate_or_download_documents_yet` (parametrized) |
| `QA-090-11` | **תיקון 2 באגים אמיתיים**: שם עברי ב-PDF | עובד עם שם עברי, PDF נקרא בפועל ב-`pypdf` | הטקסט קריא (לא `■`/`�`) **וגם** בסדר הקריאה הנכון (לא הפוך) | `test_hebrew_employee_name_renders_correctly_ordered_in_the_pdf` — **שני הבאגים נמצאו באימות ידני מול סנדבוקס, לא ב-pytest** |
| `QA-090-12` | הורדה מוצלחת + audit | `GET .../download` | `200`, `content-type: application/pdf`; `audit_log` מכיל `GENERATED` **וגם** `DOWNLOADED` | `test_successful_download_returns_pdf_and_records_audit` |
| `QA-090-13` | **דוגמה מחושבת ביד** (ידני) | `G-2021-001` בסנדבוקס: 10,000 אופציות, מחיר 2.5 ILS, מענק 2021-01-15, cliff 12, סה"כ 48, נאמן "ESOP Trust Services", עובד "ישראל ישראלי" | כל 12 השדות ב-PDF תואמים בדיוק למקור ב-DB; הכיתוב "does not constitute a legally binding agreement or signature" מופיע | אומת ידנית מול סנדבוקס (טקסט ה-PDF חולץ והושווה שדה-שדה) |

### שלב 2 — תבניות נוספות

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-090-20` | נספח 102 על מסלול 102 | `SECTION_102_APPENDIX` על `IL_102_CAPITAL_GAINS` | `200` — **מחליף** את QA-090-03 של שלב 1 (שציפה ל-400 "טרם מומש"), שינוי התנהגות מכוון | `test_section_102_appendix_generates_for_an_il_102_grant` |
| `QA-090-21` | נספח 102 על מסלול אמריקאי | `US_ISO` | `409` — אי-תחולה, לא הכרעת מס חדשה | `test_section_102_appendix_is_rejected_for_a_us_grant` |
| `QA-090-22` | נספח 102 בלי נאמן | מענק 102 בלי `trustee_id` | `409` | `test_section_102_appendix_is_rejected_without_a_trustee` |
| `QA-090-23` | **סימון דמו בגוף ה-PDF** | חילוץ טקסט מה-PDF | הטקסט מכיל `DEMO TEMPLATE` **וגם** את הצהרת אי-המחויבות — לא רק הערה בקוד | `test_section_102_appendix_pdf_is_marked_as_a_demo_template` |
| `QA-090-24` | אישור הפקדה בלי תאריך הפקדה | מענק בלי `trustee_deposit_date` | `409` — אישור על הפקדה שלא קרתה הוא מסמך מטעה | `test_trustee_deposit_confirmation_requires_an_actual_deposit_date` |
| `QA-090-25` | אישור הפקדה אחרי שנרשמה | אותו מענק עם תאריך הפקדה | `200` | `test_trustee_deposit_confirmation_succeeds_once_the_deposit_is_recorded` |

### שלב 2 — מכונת המצבים

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-090-30` | `DRAFT → SENT` | `POST /admin/documents/{id}/send` | `200`, `status=SENT`, `sent_at` מאוכלס | `test_draft_can_be_sent` |
| `QA-090-31` | `SENT → ACKNOWLEDGED` | `POST /employee/documents/{id}/acknowledge` | `200`; `acknowledged_at` + `acknowledged_by_user_id` מאוכלסים | `test_employee_can_acknowledge_a_sent_document` |
| `QA-090-32` | **P5**: אישור פעמיים | אותה קריאה שוב | `409` · `"already ACKNOWLEDGED"` — לא אפקט שני | `test_acknowledging_twice_is_rejected_not_applied_twice` |
| `QA-090-33` | אישור מסמך שלא נשלח | `DRAFT` | `409` | `test_cannot_acknowledge_a_document_that_was_never_sent` |
| `QA-090-34` | מצב סופי | דחייה אחרי אישור | `409` · `"final"` | `test_acknowledged_is_terminal_and_cannot_be_declined_afterwards` |
| `QA-090-35` | שליחת גרסה מיושנת | `send` על `is_latest=false` | `409` · `"superseded"` | `test_sending_a_superseded_version_is_rejected` |
| `QA-090-36` | **טריגר DB**: מסמך שאושר קפוא | `UPDATE`/`DELETE` ישירים ב-SQL על שורה `ACKNOWLEDGED` | `IntegrityError` בשני המקרים | אומת ידנית מול עותק של ה-DB החי (SQL גולמי, עוקף את שכבת האפליקציה — זו כל הנקודה) |
| `QA-090-37` | הטריגר לא שובר קריאה לגיטימית | הורדת מסמך שכבר אושר | `200` — הטריגר חוסם `UPDATE` על `documents`, וההורדה כותבת רק שורת audit | אומת ידנית מול סנדבוקס |

### שלב 2 — היקף רשימות והרשאות בשלושת הפורטלים

| מזהה | מה מריצים | על מי | תוצאה צפויה | כיסוי |
|---|---|---|---|---|
| `QA-090-40` | רשימת העובד מסתירה טיוטות | `GET /employee/documents` | רק מסמכים שנשלחו בפועל; אף `DRAFT` | `test_employee_list_hides_drafts_and_shows_only_own_documents` |
| `QA-090-41` | עובד לא מוריד טיוטה | `GET /employee/documents/{id}/download` על `DRAFT` | `403` | `test_employee_cannot_download_a_draft_not_yet_sent` |
| `QA-090-42` | תור הנאמן | `GET /trustee/documents/pending` | רק `SENT` ורק של מענקים שהוא הנאמן שלהם | `test_trustee_pending_queue_shows_only_own_sent_documents` |
| `QA-090-43` | ספריית האדמין מסוננת לחברה | `GET /admin/documents` משתי חברות | אדמין A רואה; אדמין B מקבל `[]` | `test_admin_list_is_scoped_to_own_company` |
| `QA-090-44` | סינון סטטוס לא מוכר | `?status=NOT_A_STATUS` | `400` | `test_admin_list_rejects_an_unknown_status_filter` |
| `QA-090-45` | **P2/IDOR**: עובד אחר מאשר | מזהה מסמך תקין, עובד לא נכון | `403`; **המצב לא השתנה** (נשאר `SENT`) | `test_another_employee_cannot_acknowledge_someone_elses_document` |
| `QA-090-46` | **P2/IDOR**: נאמן אחר מאשר | נאמן שאינו נאמן המענק | `403` | `test_a_different_trustee_cannot_acknowledge_the_document` |
| `QA-090-47` | **מחזור חיים מלא** (ידני) | `G-2021-001` בסנדבוקס: יצירה → העובד לא רואה → שליחה → אישור → אישור שני | `[]` לפני שליחה; `409` באישור השני; ה-PDF מסומן דמו, קריא בעברית, ונושא את הצהרת אי-המחויבות | אומת ידנית מול סנדבוקס |

## (ב) אזורי סיכון

חולשות **בקוד שקיים בפועל** בלבד. אין ניחושים על פיצ'רים שטרם נבנו.

| # | הסיכון | דפוס (P1-P6) | איפה בקוד |
|---|---|---|---|
| 1 | ~~פונט ברירת המחדל של ReportLab בלי glyphs לעברית — שם עובד הודפס כריבועים~~ **נסגר** | P4 (נתון חסר/שבור מוצג כאילו תקין) | `_ensure_unicode_font` ב-`services/documents.py`; רגרסיה: QA-090-11 |
| 2 | ~~אין BIDI — טקסט עברי נצבע במראה~~ **נסגר** | P6 (נאמנות בין כתיבה לקריאה) | `_rtl()` ב-`services/documents.py` (python-bidi); רגרסיה: QA-090-11 |
| 3 | רשימת נתיבי הגופן (`_UNICODE_FONT_CANDIDATES`) — רק `C:/Windows/Fonts/arial.ttf` אומת בפועל שיש לו כיסוי עברית. שני הנתיבים האחרים (DejaVu ב-Linux, Arial Unicode ב-macOS) הם מועמדים סבירים **שלא נבדקו** | — | `services/documents.py`. אם אף אחד לא קיים → `DocumentRenderingError` מפורש, לא PDF שבור בשקט. לבדוק לפני הרצה על Linux/macOS |
| 4 | `assert_document_access` בודק `company_id`/`employee_id`/`trustee_id` על שורת `Document` עצמה, ולא דרך join ל-`Grant` | P2 | `services/document_access.py`. **בכוונה** — עמודות מוכפלות כדי שהבדיקה תהיה השוואה ישירה, בדיוק כמו `LedgerOwnership`. כל endpoint חדש חייב לקרוא לה **ראשון** |
| 5 | קבצי ה-PDF ב-`document_store/` לא ב-git ולא נשמרים בין מכונות | — | `.gitignore`. שורת `Document` תישאר עם `file_path` שמצביע לקובץ שלא קיים אחרי checkout טרי → `500` מפורש בהורדה (`"Document file is missing from storage"`), לא הורדה של קובץ ריק |
| 6 | ~~אין טריגר DB שחוסם שינוי מסמך שאושר~~ **נסגר בשלב 2** | P5 | מיגרציה `a1b2c3d4e5f6` — שני טריגרים `WHEN OLD.status='ACKNOWLEDGED'`, אותו דפוס כמו `ledger_events`. רגרסיה: QA-090-36/37 |
| 7 | `national_id` הוא PII חדש בלי בקרת גישה נפרדת — נחשף בכל PDF שאדמין יכול להוריד | — | `models.py`. הוחלט במפורש בתכנון; אם יידרש הפרדה (למשל אדמין HR בלי גישה ל-PII) — זו נקודת ההרחבה, ראו v1.3.0 (RBAC מפורט) |
| 8 | `EXPIRED` קיים ב-`DocumentStatus` ובמכונת המצבים, אבל **אין שום קוד שמייצר אותו** — אין scheduler ואין מדיניות תפוגה | — | `services/document_status.py`. מכוון: המצב מוגדר כדי שהמכונה תהיה שלמה, אבל מדיניות ("אחרי כמה ימים פג?") היא החלטת מוצר שטרם התקבלה. אין להציג ל-UI ספירת ימים לתפוגה עד שתתקבל — אחרת נרמז דיוק שלא קיים |
| 9 | גם עובד וגם נאמן יכולים לאשר את אותו מסמך, והראשון "תופס" (השני מקבל 409) | P3 | `routes.py` — שני נתיבי אישור נפרדים, אותה פונקציית מעבר משותפת (`_transition_document`), כך שהוולידציה זהה בשניהם. מי *אמור* לאשר כל סוג מסמך היא החלטת מוצר לשלב 3 (ה-UI), לא פער טכני |
| 10 | דחייה (`DECLINED`) לא דורשת סיבה | — | `routes.py`. דחייה בלי סיבה חסרת ערך תפעולי למי שצריך לטפל בה. הועלה בסקירת ה-UX; לשקול שדה סיבה בשלב 3 |
