# HANDOFF ארכיון — v1.1.0 — דוחות, ייצוא ו-BI

> שוחררה 14/08/2026. שבעה דוחות, לוח אנליטי, CSV/PDF, דוחות שמורים.
>
> **ארכיון. אל תקרא אותו בשגרה** — הוא כאן לטריאז' היסטורי בלבד.
> המצב הפעיל יושב ב-[`HANDOFF.md`](../../HANDOFF.md) בשורש.
> הועתק מילה-במילה מ-`HANDOFF.md` בפיצול 14/08/2026, ללא עריכת תוכן.

---

> **v1.1.0 שוחררה (14/08/2026) - push, תיוג, והחלת המיגרציה על ה-DB החי.**
> שלוש הפעולות שנותרו מהבלוק הקודם בוצעו, כל אחת אחרי בקשה מפורשת נפרדת של
> המשתתף בתורה נפרדת, לפי אותו פרוטוקול כמו כל גרסה קודמת.
>
> **push**: `main` המקומי היה 10 לפני / 0 מאחור את `origin/main`, כלומר
> **fast-forward טהור - לא נדרש merge ולא נוצר merge commit** (המשתתף ביקש
> "מרג"; המצב בפועל לא חייב כזה, וזה נאמר לו). `f7f9dd3..c897fbd`. הסוויטה
> המלאה הורצה כשער אחרון *לפני* הדחיפה: **532 עוברות, 0 נכשלות** - זהה למה
> שתועד בבלוק הקודם, אין רגרסיה. אין branch/PR - המשך אותה סטייה מודעת
> שקיימת מ-v1.0.2 (commit ישיר ל-`main`).
>
> **תג**: `v1.1.0` annotated על `c897fbd` (ראש `main`), באותה מוסכמת ניסוח
> כמו `v1.0.1`/`v1.0.2` (שורה אחת שמסכמת את תוכן הגרסה). נדחף ואומת מול
> ה-remote ב-`git ls-remote --tags` (`refs/tags/v1.1.0^{}` = `c897fbd`),
> לא רק בהסתמכות על פלט ה-push.
>
> **ה-DB החי עלה ל-head**. גיבוי לפני:
> `db_backups/esop_database.20260814-024755.db`. `alembic upgrade head`
> (`f4b8a2d6e1c9` → `23416d683773`) - שקט. **המיגרציה נבדקה לפני ההרצה
> ואומתה כ-`CREATE TABLE` טהור + אינדקס על הטבלה החדשה בלבד**, בלי `ALTER`
> על טבלה קיימת - כלומר תרחיש הכשל של `bd65db40f654` (batch-recreate עם
> DROP על טבלה שהיא FK target) לא רלוונטי כאן מבנית, לא רק "לא קרה".
> אומת אחרי: `alembic_version = 23416d683773`, `integrity_check: ok`,
> `foreign_key_check: []`, אפס טבלאות `_alembic_tmp%` תקועות, וכל הספירות
> זהות לפני/אחרי (25 companies, 260 employees, 19 pools, 251 grants,
> 1035 ledger_events, 55 users) - אפס דריפט. `saved_reports` קיימת וריקה
> עם שני האינדקסים הצפויים (`ix_saved_reports_company_id` +
> ה-autoindex של ה-UNIQUE), כצפוי - אף UI לא כתב אליה לפני המיגרציה.
>
> **הערה על ההקשר**: השיחה הזו נפתחה כ"סקירה מלאה של הפרויקט", המשתתף
> עצר אותה אחרי קריאת `HANDOFF.md`/`VERSION`/מצב git בלבד, והפנה את השיחה
> לשלוש פעולות ה-git/DB. **הסקירה עצמה לא בוצעה** - אם היא עדיין רצויה,
> היא משימה פתוחה לשיחה חדשה.

> **v1.1.0 (14/08/2026) - שלושת השלבים שנותרו הושלמו: frontend, QA, וסקירה
> עצמאית. הגרסה שלמה מבחינת תוכן; נותרו רק פעולות git והחלת המיגרציה.**
>
> **שלב 3 - frontend.** טאב "דוחות ואנליטיקה" ב-`index_manage.html` (nav +
> `<section id="tab-reports">` + branch ב-`switchTab`) עם שני תתי-מסכים
> (טבלאות / לוח אנליטי), ו-`clients/shared/reports.js` חדש (IIFE על
> `window.ESOPReports`, אותו דפוס כמו `export_import.js`) לשכבת הרינדור.
> שני גרפים בלי ספריית צ'ארטינג: bar מוערם לפילוח מס, ו-SVG polyline
> לעקומת הבשלה בעוטף `dir="ltr"` מפורש עם התוויות העבריות **מחוץ** למערכת
> הקואורדינטות. **שני סוכני frontend נפלו באמצע** (מגבלת סשן) - הראשון
> השאיר את כל שמונת ה-`onclick` handlers **לא מוגדרים**, כלומר טאב שקורס
> ב-`ReferenceError` בלחיצה. נמצא בבדיקה בפועל (grep על כל handler מול שני
> הקבצים), לא בהסתמכות על דיווח הסוכן, והושלם בהרצה שנייה.
>
> **שלב 4 - QA.** `tests/test_reports.py` - 112 בדיקות (98 + 14 שנוספו
> אחרי הסקירה), ממופות ל-`QA-110-01..44`. הסוויטה המלאה: **532 עוברות,
> 0 נכשלות** (הייתה 420). `docs/qa/v1.1.0.md` חדש, `QA_TESTBOOK.md` עודכן
> (v1.1.0 פעילה, v1.0.2 לארכיון). **`VERSION` → `1.1.0`** - לא כחלק
> מטקס שחרור אלא כי `test_version_file_is_not_behind_the_qa_testbook`
> נכשל בפועל ברגע שהטסטבוק סימן את v1.1.0 כפעילה; 15/15 אינווריאנטים
> עוברים אחרי הבאמפ.
>
> **אימות אי-ריקות - שמונה שבירות מכוונות, כל אחת נכשלה בפועל ושוחזרה**
> (הסוכן נעצר בדיוק על השלב הזה, וזה מה שבאמת היה חסר): הסרת מגן ה-
> look-ahead, ביטול בדיקת המטבע, `writerow` בלי `_escape_formula_cells`,
> נראות דוח שמור לפי `company_id` בלבד, טוען פולים שמתעלם מ-`CompanyScope`,
> עמודה כספית ב-checklist של ASC 718, הסרת `columns` מהמעטפת, והשמטת
> `basis` מ-`columns`. **לקח תפעולי**: `git checkout --` לשחזור קובץ עם
> שינויים לא-מקומיטים מוחק אותם - כך אבד זמנית תיקון המעטפת והוחזר. לשחזור
> עבודה לא-מקומיטת יש להשתמש בעותק שמור, לא ב-HEAD.
>
> **אימות ידני מול `qa_sandbox.db`** (נזרע מחדש, כולל `23416d683773`;
> ה-DB החי לא נגע): שבעת הדוחות נטענים, ולידציית טווח-תאריכים חוסמת בצד
> לקוח, badge "אומדן" נבדל מ"לא זמין" באייקון+טקסט, CRUD מלא של דוחות
> שמורים דרך ה-UI, CSV/PDF תקינים. **עברית ב-PDF אינה מראה - אומת
> בבדיקה דיפרנציאלית** ולא ב"נראה בסדר": ה-PDF האמיתי נחלץ בסדר לוגי
> ואילו PDF שנבנה במכוון הפוך נחלץ בסדר ויזואלי, כלומר השניים ניתנים
> להבחנה וה-`_rtl`/`get_display` (`documents.py:182`) אכן מוחל.
>
> **שלב 5 - סקירה עצמאית (change-reviewer): `PASS`, אפס חוסמים**, אחרי
> שאימת בעצמו את הסוויטה (518 באותו רגע) ושחזר בעצמו שתי שבירות. ארבע
> אזהרות לא-חוסמות, **שלוש מהן תוקנו כאן**: (1) `columns` לא נשלח במעטפת
> ה-JSON והמסך גזר כותרות מ-`Object.keys(rows[0])` - שני מקורות שחייבים
> להישאר מסונכרנים ידנית, ושחיקתם מיישרת עמודת כסף מתחת לכותרת של עמודה
> אחרת. תוקן בשרת ובלקוח + שתי בדיקות רגרסיה (`QA-110-42`) שנועלות גם את
> אבידת-הדאטה-בשקט של `extrasaction="ignore"`. (2) התכנון דרש שה-UI יתמוך
> בסימון "משותף לחברה", אבל `is_private: true` היה קבוע בקוד - נוספה תיבת
> סימון. (3) תוויות ציר ה-X בעקומה גלשו מהעוטף וחפפו ללג'נד (נמדד בדפדפן:
> חריגה 15px, חפיפה 7px) - תוקן ל-`top: calc(100% - 24px)`. האזהרה
> הרביעית (הקו המלא בעקומה כמעט אף פעם לא מצויר) נשארה פתוחה ומתועדת
> כסיכון 8. **הסוקר גם תיקן אותי**: סיכון 8 טען שהלג'נד מבטיח קו שאינו
> קיים - בפועל הלג'נד מותנה נכון ב-`hasSolidSegment`; הניסוח תוקן.
>
> **git**: אין commit חדש. כל העבודה של השיחה הזו ב-working tree.

> **13/08/2026 (שיחה נפרדת, לא נוגעת ב-v1.1.0) - תחזוקת תיעוד: `/init` +
> ניקוי שורש הריפו.** שיחה עצמאית שלא ממשיכה את בניית v1.1.0 - מצב הגרסה
> בבלוק הבא למטה לא השתנה כלל (עדיין DB+backend בלבד, אותם שני commits
> מקומיים). `/init` הורץ; `CLAUDE.md` כבר היה קיים ומפורט, אז זה היה שיפור
> ולא כתיבה מאפס. **ממצא אמיתי שתוקן**: `CLAUDE.md` תיאר את שכבת ה-API
> כקובץ יחיד `backend/app/api/routes.py` - קובץ כזה כבר לא קיים; ה-API
> בפועל הוא 15 ראוטרים לפי דומיין, מורכבים ב-`main.py:47-61`.
>
> **`ARCHITECTURE.md` חדש** - מפת קוד סטטית (לא מסמך מצב, לא מתעדכן כל
> גרסה) שחסרה עד עכשיו: מנגנון ה-ledger מבוסס-האירועים
> (`append_event`/`project`/`PROJECTORS`), תבנית `company_scope`
> (`TABLE_REGISTRY`/`SPECIAL_CASED_TABLES` והאינווריאנט שאוכף אותה), מלאי
> שכבות ה-API/services, מודל האימות (session-based, לא JWT), מנוע כללי-המס
> (rule packs מגורסים לפי תאריך, בלי fallback שקט), ומנוע המסמכים
> (ReportLab+RTL, לא template HTML). **כל הפניית קובץ:שורה אומתה ידנית מול
> הקוד החי** (לא רק דיווח סוכן-חקירה) - `ledger.py`, `models.py`,
> `company_scope.py`, `auth.py`, `tests/conftest.py` - כולן תואמות בדיוק
> אחרי בדיקה חוזרת.
>
> **`CLAUDE.md`**: תוקן תיאור ה-API (למעלה), נוסף פרק "מפת מסמכים" קצר
> תחת Context discipline (איזה מסמך עונה על איזו שאלה), נוספה פקודת "בדיקה
> בודדת" (`pytest tests/<file>.py::<test>`), ותועד שאין linter מוגדר
> בפרויקט. `GOAL.md`'s טבלת המסמכים קיבלה שלוש שורות חדשות
> (`ARCHITECTURE.md`/`HANDOFF.md`/`QA_TESTBOOK.md` היו חסרות ממנה).
>
> **ניקוי שורש הריפו**: שישה קבצים מקומיים, לא-במעקב git, שכבר מתועדים
> ב-`.gitignore` כחד-פעמיים/ניתנים-להשלכה
> (`esop_database.backup_20260806.db`, `esop_database.buggy_baseline.db`,
> `qa_sandbox.db`, `QA_TESTBOOK.md.bak`, `_skills_audit.ps1`,
> `_skills_audit.txt`) הועברו ל-`.trash/` חדש (git-ignored) לפי בקשת
> המשתתף - **לא נמחקו לצמיתות** (מחיקה ישירה סורבה במפורש לפי המדיניות של
> הסוכן; המשתתף אז ביקש העברה במקום, וזו בוצעה). `pytest
> tests/test_project_invariants.py` הורץ אחרי כל השינויים - 15/15 עוברות,
> ללא רגרסיה.
>
> **git**: קומיט אחד על `main` המקומי, תיעוד/ניקוי בלבד - לא נוגע בקוד
> ייצור, בסכמה, או ב-`esop_database.db`. **לא נדחף** - ממתין לבקשה מפורשת
> נפרדת, כמו כל שינוי git אחר בפרויקט הזה. `main` המקומי עדיין לפני
> `origin` גם בגלל שני ה-commits הקודמים של v1.1.0 (`05f3a4c`, `c9a4cfc`)
> שלא נדחפו.

> **v1.1.0 (13/08/2026) - תכנון מלא + שני שלבים ראשונים מתוך חמישה, שיחה לא
> סגורה בכוונה (המשתתף ביקש להפסיק כאן ולפתוח שיחה חדשה, ה-context גדל).**
> תהליך התכנון המלא לפי `CLAUDE.md` שלב 4: שני `Explore` agents מיפו את הקוד
> הקיים (דאטה/ledger/audit/export/PDF, ודפוסי UI בפורטל האדמין), אחריהם שישה
> מומחי-סקירה read-only (product/architecture/design/qa-expert +
> security-engineer + tax-domain-expert, כי הפיצ'ר נוגע בכסף/PII ובדגל ⚠️
> ASC 718 המפורש ב-`FEATURE_SPEC.md`). ארבע החלטות מוצריות הוצגו למשתתף
> (`AskUserQuestion`, אחרי שהוסבר מחדש בעברית פשוטה לבקשתו) ואושרו. תוכנית
> טכנית קונקרטית נכתבה ואושרה (`ExitPlanMode`) ב-
> `C:\Users\Shalom\.claude\plans\reactive-frolicking-cherny.md`.
>
> **ארבע ההחלטות שנקבעו** (מפורטות בתוכנית עצמה): (1) דוח מוכנות ASC 718 -
> רשימת-בדיקה בלבד, בלי מספר כסף (product-expert + tax-domain-expert הגיעו
> לאותה מסקנה בנפרד). (2) דוח "הוצאות תגמול הוני" - כן נבנה, אבל כאומדן
> מתוייג במפורש "Estimated Intrinsic Value (Not GAAP Expense)", לא כמספר
> מוסמך. (3) חשיפה לפי נאמן - ספירה (headcount+אופציות) בסבב הזה, שדה שמור
> לערך שקלי עתידי (v1.4.0). (4) דוחות שמורים - פרטי-לשומר כברירת מחדל, אבל
> הסכמה תומכת גם ב"משותף לחברה" (`is_private` flag) - היום יש חשבון מנהל
> משותף אחד לכל חברה, אז ההבחנה תתממש בפועל רק אחרי v1.5.0 (RBAC).
>
> **נוסחת "הוצאות תגמול הוני" קיבלה אישור סופי (לא רק אישור-מסגרת) מ-
> tax-domain-expert** בתחילת המימוש, עם שני תיקונים מחייבים שהוטמעו בתוכנית
> **ובקוד**: (א) רק מחיר `StockPricesHistory` **הקודם** לתאריך המענק
> (`price_date <= grant_date`) - לעולם לא מחיר עתידי (look-ahead bias),
> ולעולם לא נפילה חזרה ל-`exercise_price`. (ב) בדיקת התאמת מטבע בין
> `Grant.currency` ל-`StockPricesHistory.currency` - חוסר-התאמה הוא סיבת
> "לא זמין" שלישית ונפרדת (`CURRENCY_MISMATCH`, לצד `NO_PRICE_DATA`/
> `NO_PRECEDING_PRICE`).
>
> **שלב 1 - `SavedReport` model + מיגרציה (database-engineer, הושלם ו-
> commit מקומי `05f3a4c`).** טבלה חדשה יחידה לגרסה הזו - כל שאר v1.1.0 קורא
> מדאטה קיים בזמן קריאה. מיגרציה `23416d683773` (על גבי `f4b8a2d6e1c9`),
> **לא הוחלה על ה-DB החי** - רק אומתה upgrade→downgrade→upgrade על עותק
> סנדבוקס נפרד. `saved_reports` נרשמה ב-`company_scope.SPECIAL_CASED_TABLES`
> (לא `TABLE_REGISTRY`) - קונפיגורציית-עבודה אישית של admin, לא דאטה עסקי
> שאמור לנדוד עם ייצוא/ייבוא חברה; מצומדת ל-`owner_user_id`, ו-`users` עצמה
> כבר SPECIAL_CASED לצמיתות. **אינווריאנט
> `test_every_company_scoped_table_is_registered_or_explicitly_special_cased`
> אומת שאינו ריק** - הוסרה זמנית הרשומה, הבדיקה נכשלה בפועל, הוחזרה מיד.
>
> **שלב 2 - שירות/API של הדוחות (backend-engineer, הושלם ו-commit מקומי
> `c9a4cfc`).** קבצים חדשים `backend/app/services/reports.py` +
> `backend/app/api/reports.py`, admin-only, כל שאילתה בנויה מ-
> `company_scope.build_company_scope`'s id-sets (**לא** re-derivation עצמאי
> של `company_id` - אף אחת מ-`Grant`/`VestingSchedule`/`ExerciseRequest`/
> `ExerciseTaxRecord`/`AuditLog` אין לה עמודת `company_id` ישירה). שבעת
> הדוחות + לוח אנליטי + CRUD לדוחות שמורים - כולם ממומשים. "עובדים בסיכון
> דדליין" קורא ישירות ל-`notifications.py::for_admin` הקיים (אפס שכפול
> לוגיקת דדליין). "מוכנות ASC 718" - דגלים בוליאניים בלבד, אומת בבדיקה
> שאין שדה כסף בכלל. דוח התנועה - שני מקורות נפרדים (LedgerEvent+AuditLog),
> חלק Trustee מציג במפורש "אין מעקב" ולעולם לא מושמט. כל יצירת/הורדת דוח
> כותבת שורת `AuditLog` (מסלול bulk-egress חדש שלא היה מתועד קודם).
> **ממצא אמיתי בדרך**: `_render_pdf` הקיים צמוד ל-`(document_id, version)`
> של `Document` מתמיד ולפריסת 2-עמודות קבועה - לא מתאים לדוח טבלאי אפמרי.
> נוסף `render_tabular_pdf` חדש ב-`documents.py` (משתף רישום גופן+RTL עם
> הפונקציה הקיימת, לא משכפל).
>
> **אימות שלב 2**: `pytest` המלא - **420 עוברות, 0 נכשלות** (זהה לבסיס לפני
> השינוי, אין רגרסיה). אימות ידני נוסף (קובץ scratch זמני, נמחק אחרי -
> `tests/_scratch_verify_reports.py`, לא נכלל ב-collection ולא נשאר בעץ
> העבודה): שתי חברות עם דאטה אמיתי (כולל שם עברי לבדיקת RTL, ומענק עם
> חוסר-התאמת מטבע מכוון), 14/14 בדיקות תוצאה-בפועל (לא רק סטטוס-200) עברו,
> כולל בידוד חוצה-חברות מאומת בפועל (חברה ב' מקבלת `rows == []` על אף
> שלחברה א' יש דאטה אמיתי) ו-CSV/PDF שאכן נרנדרים.
>
> **מה עדיין לא בוצע כאן, בכוונה** (התהליך המלא בתוכנית הטכנית): frontend
> (טאב חדש בפורטל האדמין, שני גרפים ללא ספריית צ'ארטינג, מסנן טווח-תאריכים,
> UI לדוחות שמורים, badge "אומדן" נפרד מ"לא זמין"), `tests/test_reports.py`
> (qa-engineer), `docs/qa/v1.1.0.md` + עדכון `QA_TESTBOOK.md`, סקירה עצמאית
> (change-reviewer), אימות ידני מקצה-לקצה מול `qa_sandbox.db`, ו-bump
> ל-`VERSION`/תיוג/push - כל אלה ממתינים לשיחה הבאה.
>
> **git**: שני commits מקומיים בלבד על `main` (`05f3a4c`, `c9a4cfc`),
> **לא נדחפו**. `main` המקומי לפני `origin` ב-2 commits. אין branch נפרד -
> אותה סטייה מהתקדים שכבר קרתה ב-v1.0.2 (commit ישיר ל-`main` המקומי, לא
> feature branch) - לא נבחרה מחדש כאן, פשוט המשך אותו מצב.

## איפה אנחנו

| | |
|---|---|
| גרסה | **v1.1.0 — שלמה מבחינת תוכן.** כל חמשת השלבים בוצעו; נותרו git והמיגרציה החיה |
| תוכנית טכנית | `reactive-frolicking-cherny.md` |
| `VERSION` | `1.1.0` — הועלה (נדרש ע"י `test_version_file_is_not_behind_the_qa_testbook`) |
| בדיקות | `532 passed, 0 failed` (הייתה 420). 15/15 אינווריאנטים |
| סקירה | change-reviewer: **`PASS`**, אפס חוסמים. 3 מ-4 אזהרות תוקנו, הרביעית מתועדת כסיכון 8 |
| git | `main` מקומי לפני `origin` ב-4 commits. **כל עבודת השיחה הזו ב-working tree, לא מקומיטת** |
| מיגרציה | `23416d683773` (`saved_reports`), על גבי `f4b8a2d6e1c9`. **לא הוחלה על ה-DB החי** — רק על הסנדבוקס |
| DB חי | **ללא שינוי.** `esop_database.db` עדיין על `f4b8a2d6e1c9`, לא נגעו בו |
| סנדבוקס | `qa_sandbox.db` נזרע מחדש ב-14/08 (היה ב-`.trash/`), על head. `python -m tools.run_sandbox --port 8001` |

## הצעד הבא

**v1.1.0 שלמה מבחינת תוכן - לא נותרה עבודת קוד.** חמשת השלבים בוצעו
ואומתו, הסקירה העצמאית חזרה `PASS`. מה שנותר הוא **פעולות בלבד, כל אחת
בבקשה מפורשת נפרדת של המשתתף** כמו בכל גרסה קודמת:

1. **commit** - כל השינויים עדיין ב-working tree: `backend/app/api/reports.py`
   (תיקון `columns`), `clients/admin_portal/index_manage.html`,
   `clients/shared/reports.js`, `tests/test_reports.py`,
   `docs/qa/v1.1.0.md`, `QA_TESTBOOK.md`, `VERSION`, `HANDOFF.md`.
   התקדים בגרסאות האחרונות: commits נפרדים לפי דאגה (`feat(reports-ui)`,
   `test(reports)`, `docs(qa)`, `chore(release)`).
2. **push** - `main` המקומי לפני `origin` ב-4 commits גם לפני התוספת הזו.
3. **תיוג** `v1.1.0`.
4. **החלת המיגרציה על ה-DB החי** - `23416d683773`. `CREATE TABLE` טהור
   (`saved_reports` אינה FK-target של אף טבלה קיימת, ולכן אין `ALTER`/
   batch-recreate ואין צורך ב-`PRAGMA foreign_keys=OFF/ON` - אותו פרופיל
   סיכון נמוך כמו `f4b8a2d6e1c9`). **גיבוי ל-`db_backups/` לפני**, כמו
   בכל פעם קודמת. בלי זה, `GET /admin/reports/saved` ייפול על
   `no such table` ב-DB החי.

**החלטה שנלקחה בלי לשאול, שווה לדעת עליה**: `VERSION` הועלה ל-`1.1.0`
באמצע העבודה ולא כחלק מטקס שחרור - ברגע ש-`QA_TESTBOOK.md` סימן את v1.1.0
כפעילה, `test_version_file_is_not_behind_the_qa_testbook` נכשל בפועל.
זה שינוי קובץ מקומי והפיך; אף פעולת git לא בוצעה ביוזמה עצמאית.

**חוב פתוח שנוצר בגרסה הזו** (מתועד במלואו ב-`docs/qa/v1.1.0.md`, סיכונים
1-9): הקו המלא בעקומת ההבשלה כמעט אף פעם לא מצויר (סיכון 8) · `deadline-risk`
מחזיר תוצאה שונה לכל מנהל, כי הוא נגזר מהפיד המסונן של המבקש (סיכון 5) ·
`compensation-expense` יראה 0 על הדאטה הזרוע כי אין מחיר מניה שקודם למענק
(סיכון 6, ההתנהגות נכונה - שקול שורת מחיר מוקדמת ב-`seed_data`) ·
`saved_reports` לא נודדת בייצוא/ייבוא חברה (סיכון 7).

---

## הניסוח המקורי: שינוי סדר FEATURE_SPEC, שני פריטי החוב שנדחו, ולקח ה-TABLE_REGISTRY

> **`FEATURE_SPEC.md` שונתה - סדר 1.1.0–1.6.0 הוזז (13/08/2026), בשיחה
> נפרדת מזו שסגרה את v1.0.2.** בקשת המשתתף: להעדיף בסבב הקרוב פיצ'רים
> גלויים-ללקוח על פני תשתית וחישובי-מס מיוחדים. **תוכן כל גרסה לא השתנה -
> רק הסדר/המספור.** מיפוי ישן→חדש (פירוט מלא ונימוק פר-גרסה בהערה בראש
> `FEATURE_SPEC.md`): הערכות שווי 1.1.0→**1.4.0** · דוחות/BI 1.2.0→**1.1.0**
> · קשיחות/RBAC 1.3.0→**1.5.0** · אירועי חברה/סימולטור אקזיט 1.4.0→**1.2.0**
> · מיסוי רב-מדינתי 1.5.0→**1.6.0** · אינטגרציות 1.6.0→**1.3.0**. **החלטה
> נגזרת מפורשת באותה שיחה:** חוב האבטחה החי (CORS `*`+credentials, סיסמת
> ברירת מחדל קבועה `Welcome123!`, session-ים שלא נמחקים - הערה 2א
> ב-`FEATURE_SPEC.md`) **לא** יחולץ כ-patch מוקדם כפי שהמסמך הציע - נשאר
> מתועד בקבוצה של v1.5.0 (החדשה), עד סוף התור. הנימוק: שום גרסה עדיין לא
> יצאה בפועל ללקוח (טסטבד/תרגול בלבד), ולכן אין דחיפות לחלץ תיקון אבטחה
> לפני שיש מה להגן עליו. נשמר גם כזיכרון (`esop_defer_security_debt_
> until_customer_facing.md`) כדי שההצעה לא תעלה שוב בשיחה עתידית.

**שני פריטי החוב שהיו רשומים כאן (איחוד `TABLE_REGISTRY`/`_TABLE_SPECS`,
חלון אישור פר-סוג-מסמך) נסגרו בשיחה הזו** - ראו הבלוק למעלה. מקומם פנוי.

**שני פריטי חוב, נסקרו מחדש ונדחו במפורש בשיחה הזו (המשתתף ראה את ההיקף
המלא ובחר לדחות - לא נצברו בשקט):**

1. **ניתוק בין מימוש אופציות לטבלת ההון.** אישור `ExerciseRequest` לא יוצר
   `ShareIssuance`/`Shareholder` - הכרעה מוצרית מפורשת מ-v1.0.0 (מונעת
   ספירה כפולה בנוסחת ה-fully-diluted). product-expert ציין בסקירת v1.0.2
   שאין היום מסך/צורך מאומת ל"מי באמת מחזיק מניות כולל עובדים שמימשו",
   וש-מימוש נכון דורש סוג אירוע ledger חדש (ראו פריט 2 למטה) + עמודת מעקב
   על `ShareIssuance` + החלטה על נתונים היסטוריים. הוצג למשתתף - נדחה
   במפורש לסבב הזה. סיכון 8 ב-`docs/qa/v1.0.0.md`.
2. **`ShareIssuance` נשאר בעל סוג אירוע `ledger` יחיד** (סיכון 3, ללא שינוי
   בהחלטה) - `compute_cap_table_snapshot` מסנן ישירות (`issue_date <=
   as_of`) במקום `project()` per-row, שקול ל-replay מלא רק כל עוד הסיכון
   הזה פתוח. תלוי בפריט 1: היה אמור לשמש כתשתית-קדימה לפני שמימוש מתחיל
   ליצור `ShareIssuance` בפועל - מאחר שפריט 1 נדחה, אין היום צורך עסקי
   בסוג אירוע שני, ונדחה גם הוא במפורש (לא רק "נדחה כי פריט 1 נדחה" -
   נשאל בנפרד ואושר). אם/כשיתווסף (תיקון/ביטול/העברה), חובה לעבור ל-
   `project()` ב-`services/cap_table.py`, אחרת חישוב הדילול יתעלם בשקט
   מהתיקון.

**לקח מ-v1.0.2, שווה לזכור בפעם הבאה שמוסיפים טבלה חדשה עם `company_id`:**
`company_scope.TABLE_REGISTRY`/`SPECIAL_CASED_TABLES` ואינווריאנט
`test_every_company_scoped_table_is_registered_or_explicitly_special_cased`
(`test_project_invariants.py`) קיימים בדיוק בשביל זה - הוסף לטבלה הרלוונטית
ותן ל-`pytest` להצביע אם משהו נשכח, אל תסמכו על זיכרון.
