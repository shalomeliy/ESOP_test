# FEATURE_SPEC — מפת דרכים

מסמך תכנון קדימה. הגרסה הנוכחית: **0.5.1** (מקור אמת: קובץ `VERSION`).
כל גרסה כאן מוסיפה פיצ'רים, מסכים ויכולות — ומציינת במפורש **מה היא חוסכת**
(זמן עבודה ידני, או מערכת/כלי חיצוני שאפשר לוותר עליו).

> **סדר הגרסאות שונה אחרי `GOAL.md` ו-`MARKET_ANALYSIS.md`.** שלושת הפערים
> שהמחקר זיהה כגדולים ביותר — ledger מבוסס-אירועים עם שחזור נקודתי-בזמן, כלל מס
> כדאטה מגורסת, וניידות נתונים — לא היו במפה בכלל, והם קריטריונים 2, 3, 5 ו-7
> ב-`GOAL.md`. שניים מהם משנים את מודל הנתונים עמוקות, ולכן הוקדמו לפני טבלת
> ההון: כל גרסה שנבנית מעליהם מייקרת אותם. הגרסאות שהיו 0.6.0–1.3.0 הוזזו
> שלושה מקומות קדימה ושמרו על הסדר הפנימי שלהן.

---

## מוסכמות המסמך

- **מקור אמת לגרסה:** קובץ `VERSION` בשורש. עדכון שלו מתפשט אוטומטית לשרת ולשלושת הפורטלים.
- **סימון `⚠️ דורש אימות מס`** = הפיצ'ר נוגע בכלל מס/רגולציה אמיתי. לפי `CLAUDE.md` **אסור**
  להמציא או להסיק את הכלל — חייבים לאמת מול מקור רשמי או מול המשתמש לפני מימוש.
- **סימון `🧪 QA`** = הפיצ'ר נועד במיוחד להעשיר את יכולת התרגול של בדיקות.
- כל גרסה מניחה שקודמותיה מומשו, אלא אם צוין אחרת.

### מה כבר קיים היום (0.5.1) — בסיס שהגרסאות נשענות עליו
מנוע הבשלה דטרמיניסטי (כולל עצירה בעזיבה וחשבון תאריכים בטוח) · חסימת נאמן 102 ·
חלון מימוש לאחר עזיבה · טבלאות מס versioned לפי תאריך (שטוח + מדרגות) · לוג audit ·
workflow בקשות מימוש **עם ולידציות אישור** · 3 פורטלים · חיפוש fuzzy מוגבל-הרשאה ·
מיגרציות Alembic · סוויטת `pytest` · מרכז התראות (מנוע כללים + UI בשלושת הפורטלים) ·
**auth מוקשח**: CORS מוגבל, סיסמה חד-פעמית + אכיפת החלפה, נעילת חשבון, ניקוי session.

---

## v0.4.0 — יסודות הנדסיים: בדיקות, תלויות, מיגרציות

**מטרה:** להפוך את הפרויקט לניתן לתחזוקה ולשחזור לפני שמערמים עליו עוד פיצ'רים.

**יכולות חדשות**
- `requirements.txt` מאוכלס עם גרסאות נעוצות (היום הוא ריק לגמרי — `pip install -r` לא עובד).
- Alembic למיגרציות סכמה — פותר את הבעיה שכבר נתקלנו בה בפועל: `create_all()` לא מוסיף
  עמודות לטבלאות קיימות, מה שגרם לקריסת `no such column`.
- חבילת `tests/` עם `pytest` + FastAPI `TestClient`, מול DB זמני (לא `esop_database.db`).
- `pytest` בסיסי על שלושת המנועים הדטרמיניסטיים (הבשלה, מס, חיפוש).

**מסכים חדשים:** אין (עבודת תשתית).

**חוסך:** שעות דיבוג של סטיית סכמה; מבטל את הצורך "לזכור" איך להרים סביבה מאפס.

**🧪 QA:** הבסיס שמאפשר את כל שאר הגרסאות — בלי זה כל שינוי עתידי מסתכן ברגרסיה שקטה.

---

## v0.5.0 — התראות ותזכורות (Notification Center)

**מטרה:** שהמערכת תיזום פנייה למשתמש במקום שהוא יצטרך לזכור לבדוק.

**פיצ'רים**
- טבלת `Notification` + מנוע כללים דטרמיניסטי שמייצר התראות על:
  אירוע הבשלה קרוב · סיום חסימת נאמן מתקרב · חלון מימוש לאחר עזיבה שעומד להיסגר ·
  בקשת מימוש שממתינה לאישור מעל X ימים · מענק שהובשל במלואו ולא מומש.
- העדפות התראה לכל משתמש (מה לקבל, מתי).

**מסכים חדשים**
- 🔔 מרכז התראות בשלושת הפורטלים (פעמון + מונה לא-נקראו).
- מסך "העדפות התראות" באזור אישי.

**חוסך:** מבטל מעקב ידני ביומן/אקסל אחרי תאריכי מפתח; מונע איבוד אופציות בגלל
דדליין שנשכח (סיכון כספי אמיתי לעובד).

---

## v0.6.0 — Ledger מבוסס-אירועים ושחזור נקודתי-בזמן

**מטרה:** שכל מספר במערכת יהיה *תוצר חישוב* מרצף אירועים, ושאפשר יהיה לענות לא רק
"מה נכון עכשיו" אלא **"מה האמנו שנכון בתאריך X"**. זה קריטריונים 2 ו-3 ב-`GOAL.md`,
והכשל שהמחקר מצא כנפוץ ביותר בשוק (טבלאות הון שבורות).

**פיצ'רים**
- טבלת אירועים append-only: יצירת מענק, הקפאה, עזיבה, אישור הפקדה, אישור בקשה,
  תנועת פול. כל שינוי מצב הוא אירוע — לא `UPDATE` על שדה.
- **שני צירי זמן** לכל אירוע: תאריך אפקטיבי (מתי זה קרה בעולם) ותאריך ידיעה (מתי
  המערכת למדה). זה מה שמאפשר תיקון רטרואקטיבי בלי לשקר על מה שהוצג בעבר.
- תיקון = אירוע מתקן, עם הפניה לאירוע שהוא מתקן.
- שאילתת snapshot לשני התאריכים יחד.
- Backfill מהמצב הקיים, עם תאריך ידיעה = תאריך המיגרציה ותיעוד מפורש שלפניו אין
  היסטוריית ידיעה. בלי ההצהרה הזו ה-ledger מתחזה לידע שאין לו.

**מסכים חדשים**
- ציר זמן של מענק (admin) — כל האירועים שהובילו למצב הנוכחי.
- בורר תאריך ב-audit: "הצג את המערכת כפי שנראתה ב...".

**חוסך:** הופך ביקורת מחקירה לשליפה, ומבטל את השאלה "לפי מה חושב הסכום הזה בינואר".

**🧪 QA:** כל באג חישוב הופך לניתן לשחזור מדויק — הקלט המלא שמור.

**⚠️ סיכון מימוש:** שינוי מודל נתונים עמוק. מומלץ לפצל: קודם ledger, אחר כך הציר
השני. אל תתחיל את v1.0.0 (טבלת הון) לפני שזה יציב.

---

## v0.7.0 — כללי מס כדאטה מגורסת עם מקור מצוטט

**מטרה:** שאף חישוב לא יסתמך על כלל שמקודד בקוד, ושכל מספר יישא איתו את המקור שלו.
קריטריון 5 ב-`GOAL.md`. הכשל בשוק: שינוי רגולטורי דורש release.

**פיצ'רים**
- **חבילת כללים** (rule pack): תחום שיפוט + מסלול + תאריך תוקף + **מקור מצוטט** +
  הכלל עצמו כדאטה.
- `tax_engine` טוען חבילה לפי (מדינה, סוג מענק, תאריך) במקום להחזיק ענפי `if`.
- **חישוב שלא נמצאה לו חבילה נכשל במפורש** ולא נופל לברירת מחדל. זה הכלל שהופך את
  "לא ממציאים כלל מס" מהצהרה לאכיפה.
- שרשור: כל תוצאה מחזירה את מזהה גרסת הכלל שהופעל, מעל מה שכבר נרשם ב-audit.
- ניהול חבילות במסך admin — היום הכללים נזרעים רק דרך `seed_data.py`.

**מסכים חדשים**
- ניהול חבילות כללים עם ציר זמן תוקף ומקור לכל אחת.
- "חושב לפי כלל X מתאריך Y, מקור: Z" בכל תוצאת חישוב בפורטל העובד.

**⚠️ דורש אימות מס:** כל חבילה שנטענת. הגרסה הזו לא מוסיפה כללים — היא מפסיקה
לקודד אותם, ומחייבת מקור לכל אחד.

**חוסך:** שינוי רגולטורי הופך מ-release להוספת רשומה; וכל חישוב הופך לניתן להגנה
בביקורת בלי לחפש בקוד.

---

## v0.8.0 — ייצוא מלא וניידות נתונים

**מטרה:** שהלקוח יוכל לצאת בלי לבקש רשות — ושהכניסה תהיה זולה כמו היציאה.
קריטריון 7 ב-`GOAL.md`. הכשל בשוק: ייצוא מוגבל או בתשלום, ומיגרציה שדורשת שבועות.

**פיצ'רים**
- ייצוא מלא של כל הישויות **וגם של יומן האירועים** — לא תמונת מצב.
- סכמת ייבוא/ייצוא **פומבית ומתועדת** (JSON + CSV).
- ייבוא מאותה סכמה עם dry-run: דוח שגיאות שורה-שורה לפני התחייבות.
- **דוח התאמה** — הוכחה שהמספרים במערכת המקור ובמערכת היעד זהים. זה מה שהופך
  מיגרציה מפרויקט לבדיקה.
- הכל self-serve: בלי פנייה לתמיכה ובלי תשלום.

**מסכים חדשים**
- מסך ייצוא/ייבוא עם היסטוריית ייצוא.
- דוח התאמה בין שני מקורות.

**חוסך:** מבטל את עלות המיגרציה, שהיא חסם המעבר המרכזי בשוק הזה — גם פנימה וגם החוצה.

---

## v0.9.0 — מסמכים וחתימה דיגיטלית

**מטרה:** שכל מענק ייצר את הנייר שלו בתוך המערכת.

**פיצ'רים**
- מנוע תבניות מסמכים (כתב הענקה, נספח 102, אישור הפקדה בנאמנות) עם מילוי אוטומטי
  מנתוני המענק.
- ייצוא PDF.
- זרימת חתימה פנימית: שליחה לחתימה → מעקב סטטוס → אחסון החתום → רישום ב-audit.
- קישור מסמכים לישות (מענק/עובד/בקשה).

**מסכים חדשים**
- ספריית מסמכים (admin) עם סינון לפי סטטוס חתימה.
- "המסמכים שלי" (employee).
- תור חתימות ממתינות (trustee).

**חוסך:** מבטל שימוש ב-DocuSign/Adobe Sign עבור הזרימה הפנימית, ומבטל ייצור ידני של
מסמכים ב-Word לכל מענק.

---

## v1.0.0 — טבלת הון (Cap Table) וסוגי מניות

**מטרה:** לראות את התמונה המלאה של ההון, לא רק את פול האופציות.

**פיצ'רים**
- מודלים חדשים: `ShareClass` (רגילות / בכורה A/B...), `Shareholder`, `ShareIssuance`.
- תמיכה ב**כמה פולים** לחברה (היום פול אחד בפועל), כל אחד עם תוכנית ותנאים משלו.
- חישוב דילול: fully-diluted מול outstanding.
- Snapshot של טבלת ההון לתאריך נתון (היסטוריה, לא רק "עכשיו").

**מסכים חדשים**
- מסך Cap Table עם פירוט לפי בעל מניות וסוג מניה + גרף דילול.
- מסך ניהול פולים (במקום פול יחיד מרומז).
- משווה Snapshot בין שני תאריכים.

**חוסך:** מחליף את קובץ ה-Excel שמנהל את טבלת ההון — המקור הנפוץ ביותר לטעויות
בחברות בשלב מוקדם.

---

## v1.1.0 — הערכות שווי (409A / שווי הוגן) ומחירי מניה

**מטרה:** שמחיר המניה בחישובים יגיע ממקור מתועד ומתוארך, לא מהערכה חופשית.

**פיצ'רים**
- ניהול הערכות שווי עם תאריך תוקף, מקור, וקובץ מצורף.
- שדרוג `StockPricesHistory` לשרת גם FMV לצורכי מס וגם מחיר שוק, בהפרדה ברורה.
- התראה על הערכת שווי שפג תוקפה (משתלב עם v0.5.0).
- שרשור: כל סימולציית מס תתעד גם **איזו הערכת שווי** שימשה (בנוסף לגרסת טבלת המס
  שכבר מתועדת היום).

**מסכים חדשים**
- מסך ניהול הערכות שווי (admin) עם ציר זמן.
- הצגת "המחיר מבוסס על הערכה מיום X" בסימולטור העובד.

**⚠️ דורש אימות מס:** מהו תוקף הערכת 409A ומתי היא מחייבת רענון — לאמת מול מקור, לא להסיק.

**חוסך:** מבטל מעקב ידני אחרי תוקף הערכות; הופך כל חישוב מס לניתן להגנה בביקורת.

---

## v1.2.0 — דוחות, ייצוא ו-BI

**מטרה:** שהנתונים יֵצאו החוצה בפורמט שאפשר להגיש/לנתח, בלי שאילתות ידניות.

**פיצ'רים**
- מחולל דוחות עם פילטרים שמורים: הוצאות תגמול הוני · מצב פולים · חשיפה לפי נאמן ·
  עובדים בסיכון דדליין · פעילות מימושים לתקופה.
- ייצוא CSV / Excel / PDF.
- דוח תנועה תקופתי (מה השתנה בין תאריך לתאריך) מבוסס ה-audit log הקיים.
- דוח מוכנות ל-ASC 718 ⚠️ **דורש אימות** של שיטת ההפחתה לפני מימוש.

**מסכים חדשים**
- בונה דוחות (admin) + דוחות שמורים.
- דאשבורד אנליטי עם גרפים (פילוח לפי מסלול מס, עקומת הבשלה צפויה קדימה).

**חוסך:** מבטל ייצוא ידני ל-Excel לכל בקשת הנהלה/רו"ח, ומבטל צורך בכלי BI חיצוני
לשאלות הנפוצות.

---

## v1.3.0 — קשיחות, אבטחה וכשירות לייצור 🎯

**מטרה:** אבן דרך — המערכת עומדת בסטנדרט של מערכת אמיתית, לא סנדבוקס.

> **v0.5.1 (patch אבטחה) כבר סגרה ארבעה מהפערים שתוכננו כאן**, כי הם היו קטנים
> ובלתי תלויים בשאר הגרסה: CORS מוגבל למקורות מפורשים (לא עוד `* + credentials`),
> ביטול הסיסמה הקבועה `Welcome123!` לטובת סיסמה חד-פעמית מוגרלת + אכיפת החלפה
> (`must_change_password`, נאכף ב-`require_roles`), ניקוי session-ים שפגו (נקרא
> מכל login), ונעילת חשבון + rate limiting אחרי 5 כשלונות (15 דקות). מה שנשאר
> כאן הוא רק מה שדורש שינוי מודל הרשאות עמוק יותר.

**פיצ'רים**
- הרשאות מפורטות (RBAC) מעבר ל-3 תפקידים: למשל רו"ח בקריאה-בלבד, מנהל HR בלי גישה לכספים.
- לוגים מובנים + request-id למעקב.

**מסכים חדשים**
- ניהול משתמשים והרשאות (admin).
- מסך "פעילות אבטחה": כניסות, כשלונות, session-ים פעילים.

**🧪 QA:** הבאגים המכוונים (IDOR, אישור מעל vested, קריסת 29/2) כבר תוקנו ב-v0.5.0
(ראו `QA_TESTBOOK.md`) - ההיסטוריה הבאגית שמורה בתג `qa-buggy-baseline-v1`, לא כאן.

**חוסך:** מבטל את הצורך במערכת ניהול משתמשים חיצונית לתפקידים הנפוצים.

---

## v1.4.0 — אירועי חברה: M&A, האצה, ורכישה חוזרת

**מטרה:** לתמוך באירועים שמשנים את כל טבלת ההון בבת אחת.

**פיצ'רים**
- מנוע אירועי חברה: אקזיט/מיזוג · פיצול מניות · סבב גיוס חדש.
- האצת הבשלה: single-trigger ו-double-trigger, כתנאי תוכנית ברמת המענק.
- רכישה חוזרת (buyback) של מניות מעובדים.
- **סימולטור אקזיט**: "אם החברה נמכרת ב-X, כמה כל בעל מניות מקבל" (waterfall
  לפי סוגי מניות מ-v1.0.0).

**מסכים חדשים**
- אשף אירוע חברה (admin) עם תצוגה מקדימה לפני ביצוע.
- מסך "מה יקרה לי באקזיט" (employee).

**⚠️ דורש אימות מס:** יחס ההעדפה (liquidation preference) והשלכות המס באקזיט —
לא להסיק, לאמת מול תנאי התוכנית האמיתיים.

**חוסך:** מבטל מודל Excel נפרד לכל תרחיש אקזיט, ומונע חישובי waterfall ידניים שגויים.

---

## v1.5.0 — העמקת מיסוי רב-מדינתי

**מטרה:** לסגור את הפער שבו רק `IL_102_WORK_INCOME` מקבל מודל מס מלא.

**פיצ'רים**
- ⚠️ **כל סעיף כאן דורש אימות מול מקור רשמי לפני מימוש** — הרשימה מתארת *מה חסר*,
  לא את הכלל עצמו:
  - מגבלת ה-$100K ל-ISO ומעקב חריגה.
  - AMT ל-ISO.
  - מכירה מוקדמת פוסלת (disqualifying disposition) ומעקב תקופות החזקה ל-US.
  - הפרדה בין מס בעת המימוש למס בעת המכירה ל-NSO.
- מטבע רב-לאומי עם שערי חליפין מתוארכים (היום `currency` הוא שדה טקסט בלבד, בלי המרה).
- דוח חבות מס צפויה לעובד לפי שנת מס.

**מסכים חדשים**
- מסך ניהול טבלאות מס (admin) — היום הן נזרעות רק דרך `seed_data.py`.
- "תחזית המס שלי" (employee) עם פירוט לפי אירוע.

**חוסך:** מבטל התייעצות חוזרת עם רו"ח לשאלות סטנדרטיות, ומייתר גיליון חישוב מס נפרד.

---

## v1.6.0 — אינטגרציות וייבוא/ייצוא המוני

**מטרה:** שהמערכת תתחבר למה שכבר קיים בארגון במקום לדרוש הזנה כפולה.

**פיצ'רים**
- ייבוא המוני מ-CSV/Excel (עובדים, מענקים) עם אימות מקדים ודוח שגיאות לפני התחייבות.
- API ציבורי מתועד + מפתחות API לצריכה חיצונית.
- Webhooks על אירועים (מענק נוצר, בקשה אושרה, הבשלה הושלמה).
- שכבת סנכרון HR: ייבוא עובדים/עזיבות ממערכת שכר — פותר ישירות את תרחיש ה-QuickTurn
  שבו עזיבה לא עברה דרך ה-API והפול נשאר לא מסונכרן.
- ייצוא לחשבות שכר לצורך ניכוי במקור.

**מסכים חדשים**
- אשף ייבוא עם תצוגה מקדימה ודוח שגיאות שורה-שורה.
- מסך ניהול אינטגרציות (מפתחות, webhooks, לוג סנכרון).

**חוסך:** מבטל הזנה כפולה מול מערכת השכר/HR — מקור הפער התפעולי הנפוץ ביותר במערכות
ESOP אמיתיות.

---

## סיכום: מה כל גרסה מחליפה

| גרסה | מחליף / חוסך |
|---|---|
| 0.4.0 | סביבה לא ניתנת לשחזור, דיבוג סטיית סכמה |
| 0.5.0 | מעקב ידני ביומן/אקסל אחרי דדליינים |
| 0.6.0 | חקירה ידנית בביקורת: "לפי מה חושב המספר הזה, ומתי" |
| 0.7.0 | release על כל שינוי רגולטורי + חיפוש הכלל בקוד |
| 0.8.0 | פרויקט מיגרציה (פנימה והחוצה) ונעילת ספק |
| 0.9.0 | DocuSign לזרימה פנימית + ייצור מסמכים ב-Word |
| 1.0.0 | קובץ Excel של טבלת ההון |
| 1.1.0 | מעקב ידני אחרי תוקף הערכות שווי |
| 1.2.0 | ייצוא ידני ל-Excel + כלי BI חיצוני |
| 1.3.0 | מערכת ניהול משתמשים/הרשאות חיצונית |
| 1.4.0 | מודל Excel נפרד לכל תרחיש אקזיט |
| 1.5.0 | גיליון חישוב מס + התייעצויות חוזרות |
| 1.6.0 | הזנה כפולה מול מערכת HR/שכר |

---

## הערות למימוש

1. **סדר הגרסאות אינו מקודש**, אבל 0.6.0–0.8.0 כן: הן משנות את מודל הנתונים ואת
   מנוע החישוב, וכל גרסה שנבנית מעליהן מייקרת אותן. זו הסיבה שהוקדמו.
2. **הגרסאות הכבדות: 0.6.0, 1.0.0 ו-1.4.0** (משנות את מודל הנתונים עמוקות) — שוות
   פיצול לתת-גרסאות אם הן מרגישות גדולות מדי.
2א. **חוב אבטחה חי שממתין ל-1.3.0:** CORS `*` עם credentials, סיסמת ברירת המחדל
   הקבועה `Welcome123!`, ו-session-ים שלא נמחקים אף פעם. ארבעת התיקונים האלה קטנים
   ובלתי תלויים בשאר 1.3.0 — שווה לשקול להוציא אותם כ-patch מוקדם ולא לחכות שבע
   גרסאות. ראו נספח ג ב-`QA_TESTBOOK.md`.
3. **כל פיצ'ר עם `⚠️` חוסם מימוש** עד לאימות הכלל מול מקור אמיתי — זה כלל הפרויקט
   (`CLAUDE.md`), לא המלצה.
4. **לעדכן את `VERSION` בכל השלמת גרסה**, ולתעד כאן מה בוצע בפועל מול מה שתוכנן.
