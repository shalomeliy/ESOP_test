# ESOP Enterprise Engine & Testbed — תלויות הפרויקט
#
# מדיניות: נועצים **תלויות ישירות בלבד** (מה שהקוד מייבא בפועל או מה שמריץ את המערכת),
# ולא pip freeze מלא. הסיבה: קובץ קריא שאפשר לתחזק ידנית. תלויות עקיפות
# (starlette, pydantic-core, anyio, httpcore, greenlet, Mako) נגזרות מהחבילות שלמטה —
# נעיצה כפולה שלהן רק תייצר קונפליקט גרסאות בכל שדרוג עתידי.
#
# כל הגרסאות מאומתות מול Python 3.14 ומול פתרון תלויות אמיתי (pip install --dry-run).
# התקנה: pip install -r requirements.txt

# ---------- Runtime ----------

# שרת ה-API עצמו + שרת ה-ASGI שמריץ אותו (python -m uvicorn backend.app.main:app).
fastapi==0.139.2
uvicorn==0.51.0

# ORM ומודל הנתונים. נעוץ ל-2.0.x כי models.py כתוב ב-SQLAlchemy 2.0 style;
# ירידה ל-1.4 תשבור את המודלים, ו-Alembic מייצר מיגרציות מול אותו metadata.
SQLAlchemy==2.0.51

# נעוץ במפורש למרות ש-fastapi מושך אותו ממילא: schemas.py מייבא pydantic ישירות,
# וסכמות ה-API הן חוזה חיצוני — לא רוצים שגרסת הוולידציה תזוז בשקט מתחתינו.
pydantic==2.13.4

# מיגרציות סכמה. הכרחי כי create_all() לא מוסיף עמודות לטבלה קיימת —
# זה בדיוק מה שגרם בעבר לקריסות "no such column" מול esop_database.db הקיים.
alembic==1.18.5

# יצירת PDF למסמכי מענק (v0.9.0). נבחר על פני WeasyPrint: WeasyPrint דורש
# ספריות מערכת נייטיב (Pango/Cairo/GDK) שמותקנות גרוע ב-Windows ולא נעצרות
# ע"י pip - בדיוק הפרה של "keep it runnable with uvicorn --reload בפקודה אחת".
# ReportLab הוא wheel טהור-Python בלי תלות נייטיב, זהה בהתקנה בכל מערכת הפעלה.
# pillow נמשך אוטומטית כתלות עקיפה - לא נעוץ בנפרד, כמו starlette/pydantic-core.
reportlab==5.0.0

# ReportLab מצייר תווים בסדר שהוזן, בלי אלגוריתם BIDI - שם עברי (סדר קריאה
# מימין-לשמאל) נצבע *הפוך* (מראה) בלי זה. *** נמצא באימות ידני *** מול
# G-2021-001 (עובד "ישראל ישראלי" נראה כמראה בפלט). python-bidi בלי תלויות נוספות.
python-bidi==0.6.11

# מסד אזורי הזמן של IANA. **חובה ולא נוחות**: ל-Windows אין מסד כזה, ולכן
# ZoneInfo("Asia/Jerusalem") נכשל שם בלי החבילה - ואיתו business_today(), השעון
# שכל גבול מזכה במערכת נשען עליו (backend/app/types.py). בלינוקס המערכת מספקת
# מסד משלה, אבל נעיצה כאן מבטיחה שאותה גרסת אזורי זמן רצה בכל הסביבות: כללי
# שעון קיץ *משתנים* בפועל, וישראל שינתה את שלה יותר מפעם אחת.
tzdata==2026.2

# ---------- Dev / Test ----------

# מריץ את חבילת tests/ (המנועים הדטרמיניסטיים: הבשלה, מס, חיפוש).
pytest==9.1.1

# נדרש על ידי TestClient של FastAPI/Starlette — ה-TestClient הוא עטיפה סביב לקוח httpx,
# ובלעדיו כל בדיקת endpoint נופלת כבר בשלב ה-import.
httpx==0.28.1

# קורא טקסט מ-PDF שנוצר בבדיקות (v0.9.0) - מוודא בפועל שטקסט עברי לא מודפס
# הפוך/כריבועים, לא רק ש"הקובץ נוצר". לא נדרש בזמן ריצה, רק ל-tests/.
pypdf==6.14.2
